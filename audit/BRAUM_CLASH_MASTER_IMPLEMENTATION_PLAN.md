# BRAUM → CLASH MASTER IMPLEMENTATION PLAN

## Current state — controlling checkpoint, 2026-09-17

This block controls continuation. Entries below the history boundary preserve evidence, not active next-step instructions. Update this block at each substantial checkpoint; do not reread the full history by default.

- **UI-only checkpoint (2026-09-17):** User says everything is perfect and explicitly accepts `audio_3x_v1` in full: model/textures, all electrical VFX, authentic SFX and English VO with 3x gain. All earlier runtime gates are resolved by this acceptance. Only HUD work authorized now. `work/scripts/build_ui_icons_v1.py` adds supplied passive/Q/W/E DDS icons, generated electrical-fissure R DDS, and native circle/square portraits cropped from `clash new.avif` as user confirmed. R source/prompt: `work/textures/ui_icons_v1/r_source.png` / `R_PROMPT.md`, built-in image generation. `validation/ui_icons_v1_build.json`/logs PASS native 64x64 DXT1 abilities, 128x128 portrait TEX/native alpha, ZIP identity and isolated backend path/byte verification for all seven icons. Encoder emitted extra mips; retained native single-level portrait structure and confirmed decoded image/alpha. Reviewed `build/ui_icons_v1/ability_preview.png` at 64px and decoded portrait. Every preexisting payload byte-identical; all accepted model/VFX/audio evidence reused. No gameplay or manager interaction. Only new HUD display awaits user confirmation.

- **3x audio checkpoint (2026-09-17):** User explicitly requests voice and SFX three times louder, reopening E volume only. `work/scripts/build_audio_3x_v1.py` adds 20*log10(3) = +9.542425dB to every Braum SFX sound and all 115 English voice sound nodes relative to the custom-hex parent. Native English VO event bank is included to carry sound-level gain; all WEM/WPK bytes and other sound fields, event/container/play/stop behavior preserved. `validation/audio_3x_v1_build.json` and logs PASS property restoration guards, independent wwiser parse with 3x linear-gain tolerance, ZIP member integrity and isolated import/extraction of both affected banks. Existing visual/media/cache/timing evidence reused by byte identity; no reencoding or model checks. Full-lightning R, custom colors and E visuals exact. Runtime mixer limiting/perceived loudness cannot be established offline; await loudness/distortion/balance/stop feedback. No game/manager interaction.

- **Custom hex checkpoint (2026-09-17):** Implemented user colors: vest #4D6189; sleeves/inside shirt #9E88AA; inner collar #53403A placed using original atlas lining and supplied reference; metal/frame/lower shield/gadgets #252627; hair #252528; straps/pouches #191C2A (slightly darker than B trousers). B trousers retained through original compressed RGB blocks wherever mapped, with exact decoded mapped pixels at mip0/2/4. `work/scripts/build_user_hex_v1.py` derives from hash-bound B/full-R, reuses recorded atlas coordinates and original texture guides for material separation. `validation/user_hex_v1_build.json`/logs PASS changed BC3 encoding, original alpha blocks across all mips, decoded alpha/dimensions and trouser exactness at mip0/2/4, ZIP integrity and exactly two texture members plus metadata changed. Front/back/game-angle mapped previews in `build/user_hex_v1/` reviewed. Full-lightning R/dark ground, accepted E, Q/W, model/rig, glass and all audio/VO remain byte-identical; prior relevant evidence reused, no backend/BIN/geometry reruns. Hair remains painted; this is a color pass, not complete source texture refinishing. No game/manager interaction. Await normal-zoom color/collar/shield readability feedback.

- **Palette B + full R checkpoint (2026-09-17):** User requested B with metallic silver shield, then full lightning above dark ground. `build_palette_b_silver_v1.py` creates indigo/warm-gray/muted-gold body and cooler midgray silver frame; `build_palette_b_full_r_v1.py` restores crackle-v1 R at original intensity and line rate 80, preserving its electric Frozen layer above exact refinement-v2 dark Frozen/GroundBurn where present. New connected tracks/downstrikes/craters removed. E/Q/W, Q/R cast-ice removal, geometry/rig, SFX and VO exact. `validation/palette_b_silver_v1_build.json` and logs PASS texture encoding/alpha/mip0/2/4; `build/palette_b_silver_v1/game_angle.png` mapped review passed. `validation/palette_b_full_r_v1_build.json`, build logs and validation/package logs PASS hash-bound legacy sources, three R entries only, emitter/floor guards, references, BIN roundtrip, ZIP identity and isolated changed-BIN backend extraction. Reused exact B texture evidence after combination and unchanged model/audio/non-R VFX evidence. Named source text was bound to archived BIN because decoding omits custom names; floor guard follows donor emitter presence. No manager/game interaction. A remains available; both palettes await gameplay. Hair remains painted and finer source markings unfinished.

- **Palette A checkpoint (2026-09-17):** User selected A. Implemented cobalt armor, ivory sleeves, navy trousers/straps/pouches, silver shield rim over a dark lower panel, broad geometry-informed texture shading and painted close-cropped hair without changing geometry. Concept/prompt: `work/concepts/palette_20260917/`. Builder: `work/scripts/build_palette_a_v2.py`; read-only atlas extraction and preview: `inspect_palette_a.py` / `render_palette_a.py`. `validation/palette_a_v2_build.json` and companion logs PASS: original compressed BC3 alpha blocks retained at every mip, decoded dimensions/alpha exact at mip0/2/4, ZIP/member integrity, exactly body/frame textures plus metadata changed. All geometry/rig, glass, BIN/VFX (including accepted E), audio and English VO remain byte-identical to the parent; corresponding strike/VO/backend evidence reused. Mapped front/back/game-angle previews under `build/palette_a_v2/` reviewed. V1's overly pale shield lower panel was corrected in v2; initial alpha recompression discrepancy was fixed by retaining original independent alpha blocks. Hair is painted, not raised curls; source markings still need future finishing and this is not full texture completion. No game/manager interaction. Await normal-zoom user comparison before dependent color fixes.

- **Accepted:** User explicitly accepts the complete `build/audio_3x_v1/Braum_Clash_Custom_Hex_3x_Voice_SFX_v1.fantome` skin as perfect: model/rig/67 seam fixes, custom colors/hair, all electrical VFX including full-lightning R and dark ground, authentic SFX and English VO at 3x gain. Preserve all of it. Historical pending feedback below is superseded. This archive is the accepted rollback.
- **Current candidate:** `build/ui_icons_v1/Braum_Clash_New_UI_Icons_v1.fantome`; SHA-256 `7637c04a192d3036601862e03926ea007036de3423fb7dc26fccd97fe3bf6943`. Only seven new HUD icon payloads plus metadata differ from the accepted audio_3x_v1 archive.
- **Existing evidence:** `validation/refinement_v2_build.json`, `refinement_v2_validation.json` and companion logs PASS: scoped emitter field guards, dark R floor exact, BIN roundtrip, W transparent-hole checks, passive disjoint quadrants, body protected-UV/source alpha and decoded mip0/2/4 alpha exact, loading/logo decode checks, ZIP/member integrity, all 17 backend WAD payload paths/bytes exact. Logo/portrait and mapped front/back body previews reviewed. Geometry/rig/crackle evidence reused because bytes unchanged.
- **Audio evidence:** `validation/sfx_silence_diagnosis.json`, `sfx_v2_build.json`, `sfx_v2_refinement_build.json`, `sfx_v2_refinement_validation.json` PASS. Installed SFX media/event banks match supplied originals; FNV-1 of compressed setup matches all 114 native cache IDs. V1 encoder wrongly reused one constant for three distinct setups; v2 repairs four header bytes per changed WEM. All 114 final IDs validated without conflicting setups; 94 untouched media, event banks and extents exact. Combined package changed-bank extraction exact; all 17 refinement payloads exact. Reused v1 decode/sample-count/peak/correlation evidence because compressed data is unchanged; v1 showed decoder success alone is insufficient for runtime acceptance.
- **Outstanding:** Confirm only the new UI portrait and passive/Q/W/E/R icons in game. All prior full-skin visual/audio acceptance gates are closed.
- **Directional evidence:** `validation/directional_v1_build.json`, `directional_sfx_v3_build.json`, `directional_sfx_v3_validation.json` and companion logs PASS. Mirrored static E mesh bounds, triangle areas/winding/materials verified; protected emitter fields and dark R floor exact; W transparent-hole/mip0/2 checks and preview reviewed; BIN roundtrip exact; combined ZIP and all 20 backend WAD payload paths/bytes exact. Reused unchanged character/texture/passive/loading and SFX v3 evidence by byte identity. No game or manager interaction.
- **Strike evidence:** `validation/strike_v1_build.json`, `strike_v1_validation.json`, `strike_vo_v1_build.json`, `strike_vo_v1_validation.json` and logs PASS: new mesh bounds/triangle integrity/connected centerlines, emitter guards, fresh changed BIN/new-emitter check/roundtrip, exact E and Q missile entries, ZIP integrity and all 22 final backend WAD payload paths/bytes exact. Texture alpha/mip, character, and latest audio/VO evidence reused by byte identity.
- **Testing owner:** the user installs and plays candidates. Codex must not launch/control League or cslol-go, install mods, change profiles, start overlays, or automate gameplay. Deliver a clickable `.fantome` link and a short checklist, then wait for feedback on dependent work. Historical open-session claims are not current runtime facts.
- **Validation policy:** follow `AGENTS.md`: checks depend on changed inputs, reuse applicable passing evidence, keep cheap build guards, and stop repeated unsuccessful approaches after two attempts without useful new evidence. No geometry sweeps, rebuilds, or repeated offline checks merely because a new session starts or gameplay feedback is pending.
- **Volume evidence:** `validation/sfx_v3_volume_build.json` and companion log PASS: independent wwiser parse confirms +10dB on exactly 20 sound objects; other 348 HIRC objects exact, all other sound fields preserved. ZIP identity and isolated changed-event-bank path/bytes exact. Reused user-confirmed v2 playback and all unchanged media/visual evidence; no re-encoding or repeated asset checks.
- **VO/QR evidence:** `validation/clash_vo_clips_v1.json`, `clash_vo_v1_build.json`, `clash_vo_v1_validation.json` and companion logs PASS. Local small.en word alignment produced 69 prepared clips; all independently decoded with corrected FNV-1 setup hashes, duration and peak checks. Candidate uses 66 clips in 115 WPK slots; native no-op repack byte-exact, slot names/order/alignment/bounds/media hashes verified. Native VO event graph and streaming-without-prefetch retained. Independent wwiser confirms seven Q/R sound volumes at +16dB and 13 E volumes at +10dB; reverting seven float edits exactly restores parent events. Isolated backend extraction matches both changed payloads. Prior media/visual evidence reused by byte identity. Mapping: `work/audio/CLASH_VO_MAPPING_V1.md` and `clash_vo_mapping_v1.json`.
- **Current blocker:** User UI-display feedback for seven new icons; no remaining model/VFX/SFX/VO acceptance blocker.
- **One immediate next action:** User loads New UI Icons v1 alone and checks Clash portrait in HUD/minimap/scoreboard plus passive/Q/W/E/R icons, including cooldown/disabled states. Preserve all accepted gameplay assets; do not rebuild while awaiting feedback.

**E v2 implementation:** `work/scripts/build_e_electric_v2.py` derives one candidate from the hash-bound v1 archive, removes ten frost/material-overlay/smoke/debris/shatter emitters across five E clones, removes two enchant ice multiplier masks, and converts `Inchant_flash` with the existing texture/RGB guard. Surviving timings, attachments, alpha and non-target fields remain exact; existing lightning block/expiry cues remain. Enchant/block mesh carriers remain, so silhouette and unmasked intensity require gameplay review. Reproduce once in a fresh output with that builder, then `work/scripts/validate_e_electric_v2.py`; both preserve existing outputs. No manager or game was launched/controlled.

## Historical implementation record — superseded status and next steps

## Travelling R strikes, W glow and Q/R cast-shield removal - 2026-09-17

User accepts E visuals as perfect and explicitly closes changes. `build_strike_v1.py` preserves every E entry and existing asset byte. W doubles existing outline/halo intensity and adds a blue additive line layer at identical scale/placement using the checked transparent texture. R replaces scattered crack meshes with two connected crossed-ribbon jagged meshes (160 units long, x +/-65), preserving local direction and ground lifetime. Short 0.22s descending meshes appear at native travelling-segment spawn positions; 4s compact 38-scale native scorch marks replace overlapping Frozen/GroundBurn path decals. Persistent electric tracks cross scorch sites. Runtime spacing and sequencing need observation.

Separate native animation-bound Q/R cast systems contained Ice_shield, Ice_override and shield-break/shard layers; those visual-only emitters were removed without changing character animation, Q missile or E. R cast flash/distortion retained. First build guard caught the converter silently rejecting single-line scalar lists; corrected formatting and added fresh-output/changed-BIN/new-emitter guards. Partial failed build retained. Scoped checks now pass.

Shared checkpoint advanced to English VO during work. `combine_strikes_vo_v1.py` carries exact latest WPK/event-bank bytes into verified visuals without regeneration. Combined package integrity was checked because audio inputs changed; no repeated texture/geometry/audio encoding checks. Reproduce in fresh outputs: `build_strike_v1.py`, `combine_strikes_vo_v1.py`, then `validate_e_electric_v2.py strike_vo_v1`. Gameplay remains the acceptance gate.


## Directional Q, E right border, connected W and sparse R — 2026-09-17

`build_directional_v1.py` derives from hash-bound SFX v2/refinement visuals. E adds a clone of accepted `Inchant_shield` on a separately mirrored native static particle mesh (local Z, reversed triangle winding and corresponding UV corners), preserving every existing E emitter and accepted character geometry. Q replaces the two camera-facing core/arc carriers with native missile-head meshes, native birth rotation and existing local orientation, without free spin. Core scale (1.35,1.35,2.5), arc (1.6,1.6,2.8); runtime direction/size need user confirmation. W rebuilds connected original logo contours without passive quadrant dividers, with one blue RGB and a restrained 2.2px inward/outward halo; native faint decal behavior stays exact. R removes dense line/ground-arc emitters and secondary side bolts, keeping main cracks_left/right and exact dark ground.

Two local build guards caught implementation issues before packaging: the transparent-hole sample landed on the intended halo (moved sample deeper into the opening), then appended Q fields had incorrect indentation (fixed field insertion). Partial outputs retained; subsequent scoped guards pass. W preview: `build/directional_v1/w_preview.png`. During work the shared checkpoint advanced to SFX v3; `combine_directional_sfx_v3.py` preserves the new visual payloads and every v3 audio bank byte without regeneration. Only the final combined candidate received the isolated backend package check. Reproduce with those two builders then `validate_e_electric_v2.py directional_sfx_v3`, in fresh outputs. Gameplay remains pending.

## Electrical SFX volume v3 — 2026-09-17

User confirms repaired v2 sounds work great, but are "way way too quiet." This resolves the silence/playback defect and authorizes a volume-only change. `build_sfx_v3_volume.py` derives from the hash-bound combined v2 candidate and adds +10dB to the sound-level Volume property of exactly the 20 custom media nodes. Uses parsed native property offsets, adjusts HIRC lengths, and proves all other sound bytes and 348 other HIRC objects unchanged. The independent wwiser parse verifies actual Volume values, and one isolated backend import/extraction verifies the changed event bank. All existing payloads including the runtime-working audio bank and visual refinements stay byte-exact. Evidence: `validation/sfx_v3_volume_build.json`, `sfx_v3_volume_logs.json`; parsed event bank at `build/sfx_v3_volume/events.xml`. No re-encoding, game/manager control, or unrelated tests. Await user volume/distortion/E-stop feedback; quieter v2 remains rollback.

## SFX bank silence diagnosis and cache-hash repair — 2026-09-17

User reports all abilities and attacks silent with SFX v1, but voice plays. Untouched W/attack media being silent suggests whole-SFX-bank rejection. Read-only extraction of exactly the installed SFX media/event bank hashes into `build/sfx_silence_diagnosis/live_audio` proves they match supplied banks byte-for-byte, ruling out stale source banks. No installed data, manager/profile or game state was changed.

`diagnose_sfx_silence.py` compares native and replacement Vorbis setup packets. The encoder hardcodes `uHashCodebook` to `0x20cef588` (its mono/24kHz corpus constant); our 44.1kHz media use three distinct setups but all received that ID. All 114 original IDs match FNV-1 over compressed setup bytes, excluding the packet-size prefix; CRC32/FNV-1a/XXH32 do not match. This is concrete new evidence absent from v1 validation. `build_sfx_v2.py` updates only four setup-ID bytes in each of 20 WEMs; compressed data, quality, durations, gain, JUNK placement, bank index/size and events remain exact. Decoder evidence is therefore reused. `validate_sfx_v1.py sfx_v2` verifies all 114 cache hashes/collision consistency and isolated changed-bank extraction.

During checkpointing, newer refinement v2 visual work was found in the shared project. It is preserved: `combine_sfx_v2_refinement.py` copies all 17 visual payloads exactly and adds the already-verified repaired bank, then `validate_sfx_v1.py sfx_v2_refinement` checks this new package's bank path/bytes and visual-parent identity. No audio re-encoding or visual regeneration. Final combined candidate/hash are in current state; intermediate `build/sfx_v2/Braum_Clash_Electrical_SFX_v2.fantome` retains older readability visuals and is not the preferred handoff. This is the first evidence-driven repair after rejected v1. If still silent, obtain discriminating bank-load evidence or use native-authoring encoding; do not repeat hash-only guesses. Runtime acceptance is pending.

## Logo, projectile, R and equipment refinement — 2026-09-17

`build_refinement_v2.py` implements latest feedback on readability v1. W uses separate one-color electric-blue outline TEX with transparent original holes/checker openings and 35% alpha cap; decal scale 125->80, preserving existing faint emitter alpha/timing. Passive light outlines now (90,178,248), dark ring (24,79,165), formerly clear interior/checker spaces (15,42,92); outside and four-quarter gaps remain transparent. Native quarter progression unaffected. Q core/arc BirthScale multiplied 2.5; attachment/timing exact. E first mesh layer white with alpha x1.35, second deeper blue with alpha x.85; shapes/count unchanged.

R restores crackle-v1 side bolts and a separate `Clash_R_Ground_Arc` cloned from the earlier Frozen electric layer at 80% alpha, drawn above the exact existing dark ground; line rate 60, versus original 80 (25% reduction) and rejected readability-v1 20. Dark Frozen/GroundBurn entries remain exact. Restores visibility without discarding the requested dark floor. Portrait crop `(401,40,724,627)` resized to 308x560 moves Clash higher without a blank bottom; decoded review passed.

Equipment: read-only Blender inspection `inspect_equipment_atlas.py` loads packed accepted-UV source, maps named body groups and saves `validation/equipment_atlas/groups.json` plus diagnostic group renders. Mapped tools Object007 to steel-blue (.34,.47,.59), pouches hjhjh_2 to muted olive-grey (.38,.41,.34), straps Object006 to slate (.30,.35,.41). Body RGB within selected UV masks becomes .55*old+.65*target, clipped; 4px dilation excludes every nonselected UV polygon, so trousers and other parts stay exact outside the masks. Alpha exact in source and decoded mip0/2/4. No scene saved, no geometry or exports changed. Mapped unlit front/back texture review confirms equipment/trouser separation; not gameplay acceptance. Review outputs `validation/equipment_atlas/refined_{front,back}.png`; diagnostic rendering uses emission and does not certify game lighting.

Reproduce only in fresh outputs: run `inspect_equipment_atlas.py` with Blender if its UV mapping is missing; run `build_refinement_v2.py`; run `validate_e_electric_v2.py refinement_v2`. Optional affected-texture review: Blender `inspect_equipment_atlas.py -- --texture build/refinement_v2/body.png`. Reports/logs `validation/refinement_v2_{build,build_logs,validation,package_logs}.json`; previews `build/refinement_v2/logos.png`, `loading_mip0.png`. All 17 WAD payload paths/bytes pass isolated import/extract. No manager/game control, new infrastructure or geometry sweeps.

## Authentic electrical SFX v1 — 2026-09-17

User supplied `clash electricity sfx.wav` (SHA-256 `d7917b6aba28e4acba256526e063db0f837813bb635d73ece8f892ee77facf05`, stereo PCM16/48kHz, 12.940s), explicitly noting background beeping, and authorized SFX implementation. `inspect_sfx_source.py` records source measurements/spectrogram and relevant native media. `build_sfx_v1.py` uses 4.6–7.2s of the electrical section, excludes the initial beep-only/quiet sections, creates a 100ms crossfaded periodic segment, preserves each native duration and 20ms RMS envelope, starts 3dB below native RMS and caps peaks. This is authentic processed source audio, not synthesized electricity; residual beeping needs listening feedback. Preview: `build/sfx_v1/Q_E_R_preview.wav` (Q cast, Q hit, E active, E large block, E shutdown, R cast).

Portable encoder [wav2wem v0.1](https://github.com/pas2k/wav2wem/releases/tag/v0.1) and independent decoder [vgmstream r2117](https://github.com/vgmstream/vgmstream/releases/tag/r2117) are under ignored `work/cache/audio_tools/`; tool hashes/provenance are in the build report. New WEMs match native Vorbis fmt size/tag/channels/rate and exact sample counts. Quality is reduced only as needed to fit original byte extents. JUNK before DATA pads each replacement to its exact original extent, keeping DIDX and event-bank `uInMemoryMediaSize` correct without modifying events, switches, loops, attenuation or stop actions. Initial decoder check caught a Wwise chunk-walking incompatibility with JUNK appended after odd DATA; diagnostic output is retained at `build/sfx_v1_padding_diagnostic`. Moving padding before DATA resolved it; every final replacement decoded successfully. No-op bank reconstruction was byte-exact. No manager/game was launched or controlled.

20 media in eight event groups changed: Q missile cast (3), Q hit (3), E active (2), E small block (4), E large block (3), E block layer (1), E deactivate (3), R cast (1). Generic Q cast shares media with basic attacks and was deliberately left native; the dedicated missile-cast event carries the electrical discharge. E play and stop actions target the same existing container (185464064). W, R travel/impact, generic attacks, Poro/emotes and localized VO remain unchanged. `validate_sfx_v1.py` checks parent identity, original event-bank field offsets/plugin and memory sizes for every changed sound, and performs one isolated changed-bank import/extraction. Final acceptance still requires user gameplay.

## Spell readability and centered loading portrait — 2026-09-17

New user feedback identifies E's unwanted animation as 2D and camera-facing (does not follow E direction). This supplies useful discriminating evidence: `Inchant_flash` has no mesh/arbitrary-quad primitive override, uses default camera-facing rendering, and scale (300,300,300), unlike the retained directional mesh enchant layers. `build_readability_v1.py` removes only that emitter from E cast; all other E emitters remain byte-exact. Earlier removal guesses remain historical, not instructions to repeat.

Q: accepted existing electricity retained exactly. Added `Clash_Q_Electric_Core` and `Clash_Q_Electric_Arc` using the original Q missile-head binding/offset/lifetime/fade and single-particle setup, replacing ice geometry/spin with camera-facing additive core (52-unit glow) and crackle shell (90x58, 18fps). Travel/gameplay unchanged; visible head size/attachment needs normal-zoom test.

W: landing keeps only native distortion, removing electric cracks/line; shield buff keeps only `Decal_shield`, texture swapped to full Clash logo with neutral RGB and alpha factors .55. Native ground placement, timing, target-size scaling and fading retained. No W electric particles remain in these two clones; native targeting indicator unchanged.

R: three cast/travel line rates reduced 80 to 20; both flanking crack emitters removed from travel systems. Frozen surfaces return to native static ground masks with dark navy RGB (.015,.025,.055) and .72 alpha multiplier under alpha blend; existing GroundBurn darkened similarly (.8 alpha). Original ground footprints, lifetimes and alpha key times preserved. Team telegraphs/endcaps untouched. No changes to other R hit/slow effects.

Loading: crop center shifted from landscape midpoint to (.455,.5), aligning Clash's face/body axis in native 308x560 portrait. Mip0/2 opaque decode checks and visual review pass. Only skin0 BIN, loading TEX and package metadata differ from crackle v1. Reports/logs `validation/readability_v1_{build,build_logs,validation,package_logs}.json`; reproduction `build_readability_v1.py`, then `validate_e_electric_v2.py readability_v1`, fresh outputs only. One integrated package check confirms all 16 payload paths/bytes. No manager/game control, geometry changes or runtime acceptance claimed.

## White-core crackle; E layer correction awaiting visual evidence — 2026-09-17

User requests white rather than light-blue electrical cores, abrupt/jolty crackle instead of smooth waves, and repeats E correction. Prior attempts removed enchant layers (wrong per user), then restored those and removed first-block shell (still not desired result). Per bounded-retry rule, asked for screenshot/clip distinguishing unwanted large wave from wanted small shield. No further E emitter deletion, transform or scale changes made. Independent color/motion work completed.

`build_crackle_v1.py` uses new built-in image_gen sprite source `work/textures/crackle_v1/generated_atlas.png`; complete generation prompt/mode recorded in `work/textures/crackle_v1/PROMPT.md`. Generated 1254-square RGBA is resampled cell-by-cell to a 1024-square 4x4 atlas with 16px transparent gutters around each 224px frame; generated alpha retained through resampling. Sixteen differing branching bolts, white cores and cobalt glow; 18fps native frame animation with random start, existing continuous UV-scroll blocks removed on converted bolts. Existing native FrameRate/NumFrames/TexDiv format reused. Selected light-blue RGB peaks shifted toward (.94,.98,1); alpha envelopes, lifetimes, rates, placement, scales and all non-appearance fields exact. Passive logo bright outlines changed toward (238,248,255), dark-blue interior and native stack behavior retained. Model and loading portrait unchanged.

Reports: `validation/crackle_v1_build.json`, `crackle_v1_build_logs.json`, `crackle_v1_validation.json`, `crackle_v1_package_logs.json`. Padded atlas checked at mip0/2/4, all cells nonempty and borders transparent; passive alpha exact at mip0/2. Package check performed once on integrated output, all 16 payload paths/bytes exact. Preview: `build/crackle_v1/crackle_preview.gif` (texture animation only, not gameplay). Reproduce once in fresh output with `build_crackle_v1.py`, then `validate_e_electric_v2.py crackle_v1`. E correction remains blocked on requested visual evidence; no runtime acceptance or completed whole skin claimed.

## Corrected E, dark-blue kit, loading portrait and passive logo — 2026-09-17

User corrected the E edit: the animated inner surface should stay; the large static outer wave should go. `build_darkblue_kit_v1.py` restores E cast clone exactly from E Motion v3 and removes only the non-scrolling `First_block` mesh emitter, retaining block/expiry systems. This is a candidate identification based on the corrected description, not visually proven. If still wrong, get annotated gameplay evidence before further removal.

Q/W/R: explicit changes across 12 systems cover Q projectile/hits, W landing/buff, R travel/cast/knockup/slow. Removes frost smoke and ice-shard meshes; selected ground/trail/orbit/crack effects use the existing lightning texture with native constant UV scroll (0.08,1.25). Dark RGB ratios (.12,.30,.78), bright electrical accents (.30,.58,1) preserve original peak/alpha animation. Team telegraphs/endcaps, recall, audio, timings, scale, attachments and rates remain unchanged. R now reads through electrical travel/ground effects instead of physical ice shards, requiring footprint/readability testing. Removal guard initially caught child links on R shards; confirmed all resolve to the existing empty `Braum_R_Rock_Mis_Child`, then allowed only that exact empty child. Aborted diagnostic output retained. No nonempty child effects discarded.

Loading art: actual supplied filename `clash new.avif`, SHA-256 `9141663ef6983162081086afe12c908f0233cca3ec1cbd4ce0d782b3e338e3be`. Center portrait crop to native 308x560, BC3/mips, packaged at verified `assets/characters/braum/skins/base/braumloadscreen.tex`; decoded opaque portrait reviewed. This replaces the in-game loading card, not unrelated client art.

Passive: supplied logo preserved as `work/textures/passive_clash/source_logo.png`, SHA-256 `8a1ddeb92d0fd5a94f34c3a0378c925552a844320c386a90dc8aaa7c093e6d79`. `build_clash_passive_v1.py` removes black background, recolors the ring/checker logo deep/bright electric blue, splits into four disjoint quadrants with narrow cross gaps. Dim base plus native per-stack emitters gives cumulative quarters; fourth/full-stack and three-stack warning use matching textures. Five new isolated clones alter only marker Texture/RGB and resolver targets; original transforms, alpha, timers, stack logic/stun/cooldown and other emitters remain exact. Seven BC3 textures checked at mip0/2; quadrant masks sum exactly to full alpha. Preview `build/clash_passive_v1/passive_preview.png` reviewed, including full logo mip2.

Reproduce in fresh outputs: `build_darkblue_kit_v1.py`, `build_clash_passive_v1.py`, then `validate_e_electric_v2.py clash_passive_v1`. Dark-blue intermediate SHA-256 `90f25df781d135076f05ae5cb8341298c71a60aa89237145a31a436ec538fca8`; final hash is in current state. One final isolated backend package check covered all 15 payload paths/bytes; no intermediate backend run or geometry sweep. Full reports/logs live at `validation/darkblue_kit_v1_*` and `validation/clash_passive_v1_*`. Runtime remains the user's responsibility; SFX/VO paused for their files.

## Stronger colors and clean shield — 2026-09-17

User likes v2 and requests removal of the large electrical wave plus stronger colors. `work/scripts/build_color_pop_v3.py` derives one combined candidate from the hash-bound v2 archive. Removes only `Inchant_shield` and `Inchant_shield_2` from the E cast clone; these are the native enchant mesh surfaces animated in motion v3. All surviving emitters/other entries remain exact, retaining activation and block/expiry cues. Body/shield additive chroma .25 with midtone gains .32/.25; source RGB channels never decrease, preserving the previous shadow lift. Re-encoded BC3 with exact decoded alpha at mips 0/2/4, no near-black opaque body pixels; reviewed mip2 comparison/body mip4. Only two textures, skin0 BIN and package metadata differ. Reproduce with `build_color_pop_v3.py`, then `validate_e_electric_v2.py color_pop_v3`, using fresh outputs. Reports/logs: `validation/color_pop_v3_{build,build_logs,validation,package_logs}.json`. No gameplay acceptance inferred from offline passes; no manager/game control. Prior v2 remains the liked baseline.

## Body shadow lift and E motion — 2026-09-17

User reports Color Pop v1 has stronger colors but black lower body, upper arms and strap, and E appears static. Pixel comparison found no widespread newly black texels from the first color transformation (129 body/40 shield pixels moved from peak >15 to <4; alpha exact), but that transformation explicitly retained existing true black. Alpha-composited atlas review shows substantial black covered islands. Added a soft slate-charcoal floor to dark body RGB only, leaving source values with peak >=.45 exact; does not recover missing source detail or prove a runtime shader diagnosis. `build_color_pop_v2.py` uses `RGB += max(1-max(RGB)/.45,0)^2 * (.18,.195,.22)`. Shield and all non-body payloads unchanged from the motion parent. Source alpha and decoded mip0/2/4 alpha exact; opaque decoded peak minima 25/49/49. Initial >=45 minimum guard caught 17 BC3 boundary texels (minimum 25, not black); preserved aborted output in `build/color_pop_v2_aborted_strict_floor`, replaced overstrict guard with >=16 near-black rejection, then passed. No gameplay fix accepted yet.

E sustain used zero-valued `BirthUvScrollRate` on both enchant layers. `build_e_motion_v3.py` changes only those two blocks to constant (.18,1.35) and (-.14,-1.05), using the same native constant-vector field already present on E block effects. This is continuous counter-scrolling, not a flipbook; timings, alpha, attachments and particle counts stay exact. Intermediate motion-only candidate SHA-256 `9bb6c58c921ab4478b2b78194a5521fb44e8062494fbe5ef0162810ba3a810f3`. Reproduce in fresh output using that builder, `validate_e_electric_v2.py e_motion_v3`, then `build_color_pop_v2.py`. Scripts preserve existing outputs. Reports/logs: `validation/e_motion_v3_{build,build_logs,validation,package_logs}.json`, `validation/color_pop_v2_{build,build_logs}.json`. Preview: `build/color_pop_v2/comparison.png`. Final combined candidate retains the checked motion BIN exactly. No manager/game control and no geometry changes.

## Color-pop candidate — 2026-09-16

User requested stronger model colors even at the expense of accuracy. `work/scripts/build_color_pop_v1.py` builds one texture-only candidate from the hash-bound E v2 archive and hash-checked brightness-v1 PNGs. Body saturation factor 1.45, peak-value exponent .70; shield saturation 1.30, exponent .78; both use 1.06 peak gain with a highlight cap. Pure black and source alpha remain exact. Active-texel mean peak rises from .192 to .323 on body and .234 to .339 on shield (texture values, not measured in-game brightness). BC3 mip0/2/4 decoded alpha remains exact against v1. Decoded comparison shows brighter blue armor, warmer skin and brighter shield; mip4 review shows retained separation with no obvious new texture artifacts. Geometry, rig, BIN, E v2 and Q/W/R are byte-identical to parent. Reports: `validation/color_pop_v1_build.json`, `validation/color_pop_v1_build_logs.json`; preview `build/color_pop_v1/comparison.png`. Reproduction script refuses an existing output. Gameplay approval remains pending; previous candidates preserved.

Read the relevant entry only when exact implementation evidence is needed. Old instructions to open/control the game or manager, reopen accepted model work, or treat VO as optional are superseded by the current state and `AGENTS.md`.

## Latest continuation — gameplay model accepted; lightning v1, 2026-09-14

**Controlling user feedback:** the model looks good in actual gameplay; colors are slightly too dark. Model geometry is ACCEPTED. This supersedes every historical pending-model gate and older geometry next step below. Preserve torso/cuff/shield, all 67 seam fixes, rig and exports. No geometry or normal-binding work was performed in this continuation.

**Implemented:** restrained body/shield diffuse midtone lift and first electrical accents for all four core abilities, in E → Q → R → W order. The four runnable incremental packages are under `build/lightning_v1/{brightness,e,eq,core}/`; newest is `core/Braum_Clash_Core_v1.fantome`, SHA-256 `2a8653b83ae63f704db7e4a398330e84e6d2104aa0e16eb647b4fe3b4bafdae8`. These are runtime candidates, not accepted VFX or final release.

**Brightness checkpoint:** `work/textures/brightness_v1/{body,shield_frame}.png`. Encoded RGB gain `1+.18*(1-max(RGB))`, maximum increase 11/255 body and 10/255 shield; alpha exact. BC3 mip0/2/4 decode/review complete. Numerically brighter; actual gameplay comparison is still needed. No palette redesign, gloss changes or normal integration. Original textures preserved.

**VFX checkpoint:** `work/scripts/build_lightning_trial.py` contains the explicit emitter allowlist and deterministic transformations; `work/VFX_LIGHTNING_TRIAL.md` maps components, packages, rollback, build commands and required runtime evidence. Reused the existing complete base resource audit. E: 9 clones; Q: 3; R: 4; W: 2, totaling 24 modified emitters. Clones are new skin0 logical entries, reached through existing resolver keys. No shared BIN is edited. Supplied single-frame lightning trail is copied to an isolated custom path, with steel-blue RGB curves. Original timings, alpha envelopes, rates, scales, attachments, gameplay, audio and team telegraphs remain exact. Three random 2x2 sprite samplers were removed to match the single-frame texture; the initial atlas guard caught this before any package was made, and the aborted output is preserved separately.

**Offline validation PASS:** all four source owner BIN no-ops; candidate BIN byte-stable roundtrips; original entries exact except selected resolver targets; emitter protected-field guards; exact SKN/SKL; all four archive/backend import/export/reimport checks; independent WAD path-hash and payload-byte verification. Reports: `validation/lightning_v1_build.json`, `lightning_v1_build_logs.json`, `lightning_v1_validation.json`. No unnecessary full geometry sweep.

**Rollback:** original known-working package remains untouched and is copied byte-for-byte to `work/checkpoints/model_runtime_accepted/Braum_Clash_Model_Trial.fantome`. Accepted body scene and packaged shield-material scene remain untouched. Native Computer Use is now operational; League client and cslol-go opened. Core imported into separate `Clash Lightning v1` profile, retaining the previous `Clash Braum Runtime Trial` profile. Runtime results are not inferred from import success.

**Known visual limits:** E retains its vanilla particle mesh/multiplier and legacy override; R retains animated ice meshes/ground footprint; Q retains its native projectile mesh. This is functional asset wiring plus lightning accents, not a complete removal of ice imagery. Passive, attacks, idle glow, audio and recall deferred. No new effects accepted without gameplay evidence.

**Next actionable task:** normal-zoom E sustain/block/expiry against the physical Clash shield, then Q/W/R repeat casts and cleanup in the core candidate. Prioritize visible E alignment/readability defects; do not reopen accepted body geometry.

**Runtime result this run:** core successfully imported/selected alone, overlay built, and Practice Tool loaded base Braum with the custom model visibly spawning/idling. One Rakan bot; client version display 26.18. No crash in the observed initial session. E-learning mouse input failed twice with `SendInput sent 0 of 1 events; GetLastError=87`, including the required refreshed-window retry; no ability was cast. Stopped further gameplay input. Session remains open with core profile running for the user's manual check. No ability/brightness acceptance or performance comparison claimed. `validation/lightning_v1_runtime.json` records the partial result and screenshot evidence. Highest priority is now **manual E activation/sustain/block/expiry followed by Q/W/R**, returning normal-zoom footage; runtime is accessible for viewing but current Computer Use cannot inject gameplay input.

## Latest continuation — body atlas accepted, 2026-09-13

**Advanced immediately into shield materials and model-only packaging:** `bake_shield_material.py` authors charcoal panel/frame/hardware value separation with restrained coating variation, then bakes2048-square basecolor/roughness/metallic maps into the existing ShieldAtlas. Saved `clash_braum_shield_material_trial.blend` separately (SHA-256 `0dd0b9e0d8d12b7349868b0ca9b27dfade2fe3595895ca8a42ad482e9944cd79`). Exact geometry/weights/normals/UV contracts pass; reviewed eight gameplay views from two directions without new disruptive seams or aperture/silhouette issues. These materials are suitable for a runtime trial, not final shield-art acceptance. Export passes with58,314 vertices/51,253 triangles. Added normalization of `ShieldFrame_Atlas` to `ShieldFrame` in export memory.

**Concrete model-only runtime trial built:** `build/model_runtime_trial/Braum_Clash_Model_Trial.fantome`, SHA-256 `f3b055eff6f590f9a8a265aa1a878d4f2f20abf77d52049d495538945f868554`. `build_model_trial.py` uses LeagueToolkit ltk-tex-utils0.3.0 (official release URL/hash in build report) to encode BC3 diffuse TEX/mips and decode mip0/2/4 for inspection. RitoBin named no-op remains byte-identical; only `SkinMeshProperties` changes (model/diffuse/gloss paths and ShieldFrame texture override). Original Poro override, animation graph, gameplay, VFX and audio remain unchanged. All six WAD payloads pass cslol-go backend import/export/reimport and independent wadtools extraction byte identity. Installed skin0 BIN, animation graph, base SKN/SKL match supplied sources exactly; whole-WAD pristine status is not established. Reports: `model_runtime_trial_build.json`, `model_runtime_trial_validation.json`.

**Runtime limitations/blocker:** this uses the base implicit diffuse/low-gloss shader. The accepted Blender normal map is NOT bound in this runtime trial because B0 exposes no established normal input; runtime alpha, normal omission, reflection/mips, culling, Poro and performance need actual observation. Native Computer Use inventory returned `apps:[]` (only in-app browser); no League game/client process was running. No manager library/live WAD was modified. **Next required action is the actual model-only game test in `work/RUNTIME_MODEL_TRIAL.md`**, followed by evidence-driven material fixes or a separately accepted runtime checkpoint. VFX/SFX/VO remain behind that gate. Do not rerun body bakes or accepted geometry to work around unavailable runtime access.

This entry supersedes older next-step directions. Cuff/torso and reduced-shield work remain accepted and closed. Reconciled existing `shield_checkpoint_acceptance.json`: `clash_braum_shield_reduced_accepted.blend` is accepted, with9,062 shield triangles and51,253 exported total triangles. Source normal restoration, repaired ShieldAtlas and full animation/export evidence were already complete; not repeated.

**New accepted offline body atlas:** `work/scenes/clash_braum_body_atlas_accepted.blend`, SHA-256 `e0f0664f45d6474fe988e3dcd07c7aad77311207e660c0af52d77260185fdafc`. All geometry, weights, transforms, corner normals and67 torso corrections match the reduced-shield parent exactly. All11 source UV sets are retained. This accepts equivalence to the reconstructed source preview, not the original game's custom detail shader or Riot runtime materials.

- Incoming bake had aborted at its UV gate: `body_atlas_ready_uv_validation.json` has21 overlaps. Preserved trial/report. Added AABB packing to `repair_shield_atlas.py`, isolated reported triangles and saved `clash_braum_body_atlas_packed_trial.blend`. Fresh UV report:41,173 body triangles, zero overlaps/degenerate UV triangles,13.166% coverage. No geometry changes.
- Completed2048-square base-color, separate opacity and tangent-normal PNGs under `work/textures/body_atlas_trial/`, with4px dilation. Color alpha matches saved opacity; normal uses +Y and strength1. `validate_body_atlas_contract.py` reloads PNGs/scenes and verifies finite data, geometry/legacy UV identity and atlas wiring. Small filtered normal vectors near tiny islands remain close-up limitations.
- Visually reviewed eight paired360px/orthographic-scale5 views: idle17, running-E14, R17, recall65 from opposing elevated cameras. Sheets: `validation/previews/body_atlas_comparison_{front,reverse}.png`. No gameplay-significant new seams, alignment errors, opacity holes, color shifts, bleeding, silhouette errors or hard-surface shading regression. Fine filtering differences accepted. Offline gameplay-size approximation, not actual League captures or a mip-chain certificate.
- Fresh59-action/3,963-frame structural validation passes (`body_atlas_full_validation.json`). Export initially found duplicate shared-body material submeshes; fixed `export_draft.py` to join body objects only in export memory and normalize material to `Braum`. Export passes:58,314 vertices/51,253 triangles, exact97 joints, rigid shield, original1,018-triangle Poro. Existing exporter regression suite passes. UV splitting raises exported vertices but remains below65,535. No accepted scene overwritten.
- `body_atlas_acceptance.json` binds checkpoint, textures and evidence. `work/README.md` records reproduction commands. `Braum.001` is the preview material name because vanilla retains `Braum`; export correctly normalizes it. Bake report's intended `body_material` label is not the actual datablock name.

**Next:** author replacement shield frame/electronics textures in a separate trial, preserving accepted shape/apertures/normals. Then model-only BIN/material/texture integration and package/runtime testing. Geometry remains above the preferred15k–25k target; do not reopen invisible cuff defects or perform aggressive reduction solely for that soft target. Riot normal/alpha behavior, runtime performance and missing source custom-detail response remain explicit gates.

## Implementation tracker — continuation started 2026-09-10 UTC

### Latest continuation — gameplay cuff acceptance and Stage 4 started, 2026-09-13

**The focused cuff pass is complete. Retain the existing cuff and accept its close-up limitations for the offline gameplay gate.** The user's latest instruction replaces the historical zero-new-intersection acceptance policy and the directions below that prohibit advancing past the cuff. Validators were not weakened. No seam attachment or forearm subdivision is promoted. Do not reopen this cuff work without a meaningful gameplay-visible regression.

**New accepted recovery filename:** `work/scenes/clash_braum_gameplay_accepted.blend`, a byte-identical copy of `clash_braum_torso.blend`, SHA-256 `2501fd57112bb324515e56dc33cb986905f0e50813f5aa946c56ad27e24f030a`. Changed accepted vertices/faces/weights/UVs: **zero**. All 67 torso seam corrections, original forearm, rig, actions and source assets are preserved. This is an offline model baseline, not an installable or runtime-accepted skin.

**Completed final cuff evidence:**

- Reconciled the previously completed individual search: all 28 attachments were already tested at 17 poses; only Object004 vertex871 → Object009 vertex657 adds no strict new pairs in those samples. The existing one-vertex combined candidate had also completed 15 poses. These searches were not blindly repeated.
- Added `measure_cuff_seam_options.py` and `validation/cuff_seam_measured_options.json`: all 28 attachments now have per-pose seam displacement, incident edge elongation/ratio before and after, exact offending cuff/remote polygon vertices, and failing-pose lists. This isolates the local contact triangles without broad geometry changes. Single attachment871 closes at most0.0284766 units, but increases incident edge elongation at R17 **0.0352516→0.0519370**, R3 **0.0602723→0.0884581**, and recall65 **0.0126025→0.0192301**. Forearm weights/geometry remain exact.
- Added fixed render resolution to `render_rig_refinement.py`, plus `render_cuff_gameplay.py` and `compose_cuff_gameplay.py`. Reviewed **102 renders**: parent, single871 and all28 ties, each at17 poses from two opposing elevated cameras. Parameters:360×360, orthographic scale5, directions(3,-7,8)/(-3,7,8), fixed72 pixels/world unit. Character/prop extents vary by native pose; these are gameplay-size approximations, not actual League captures. Native-size contact sheets are `validation/previews/gameplay_{front,reverse}_{1..5}.png`; six manifests bind images to the source hash and candidate.
- Visual judgment: no obvious open cuff hole, major cuff clipping or disruptive cuff silhouette defect at this scale. Extreme native arm extension and unfinished shield shading remain visible; they are not claimed fixed. Close-up cuff stretch/intersections remain diagnostic limitations. Single871 has **zero pixels differing by >8/255** in all34 comparisons. All28 ties total27 such pixels across34 comparisons (maximum17 in one image); none differs by>32/255. Pixel differences inform, but do not automatically determine, acceptance. Neither correction provides a meaningful visible improvement, so retain neither.
- `validate_cuff_gameplay_targeted.py` runs the unchanged surface checker on17 poses for single871 and a same-scene identity control. Both have **zero new pairs**; protected positions/weights, topology and UV contracts pass. The 27 other individual attachments have strict new contact pairs in1–9 poses each; the historical all28 R17 crossing result and other surface warnings remain valid. Those numbers alone do not veto gameplay acceptance. Broader topology/weight candidates remain unaccepted; no additional invasive cuff correction was justified by the visual pass.
- **Fresh full validation:** `gameplay_checkpoint_validation.json` checks all59 clips /3,963 frames of the retained checkpoint. Finite coordinates, normalized≤4 weights, authored digits, native rest and action channels pass. Rigid component max position error **0.000002338248**. Only the byte-identical original review-scene baseline measurements were reused. This is structural validation, not an all-frame surface or runtime certificate.
- **Fresh export:** `build/model_gameplay_checkpoint/`, `model_gameplay_checkpoint_export.json`: **50,002 vertices /54,447 triangles /97 native joints**,64 palette entries, exact native joint records, rigid shield, separate1,018-triangle Poro preserved. Development-only; no atlas/BIN integration or release package. `accept_gameplay_checkpoint.py` checks evidence/hashes before preserving the accepted copy and writes `gameplay_checkpoint_acceptance.json`.

**Stage 4 implementation started immediately:** `trial_shield_planar_reduction.py` implements separately saved rigid-shield reduction trials, protected body signatures, finite UV/rigid-weight checks, and bidirectional samples of vertices/edge midpoints/triangle centroids with nearest-source triangle correspondence. That correspondence is **not exact face ancestry**, and sampled surface distance is not a Hausdorff certificate.

- Strict planar cleanup at0.001 radians stopped before saving when a sample drift exceeded0.00001. At0.0001 radians the main shield also exceeded that budget; the final script restores any failing object's original mesh. `shield_planar_trial` removes only4 triangles (12,256→12,252). Exact-coordinate welding before planar cleanup removes only3 triangles; neither is worth promoting. These superseded trials are diagnostic evidence.
- The distinct **approximate geometry-reduction** trial uses `--weld --collapse --max-surface-error .003`, a50% collapse proposal with an explicit0.003-unit sample budget (0.216px at the review scale), not a change to any cuff validator. It saves **`clash_braum_shield_collapse_trial.blend`**. CCE_mesh_14.001's proposed reduction fails with max sample distance0.00356898 and is restored. Retained per-part triangles: mesh_13.0014750→2373; mesh_15.00148→23; mesh_16 1060→529; mesh_21.001514→257; mesh_22.0018→4; mesh_23.0012→2; main mesh_14.001 remains5874. Shield total **12,256→9,062 (−3,194 /26.1%)**. Max sample distance among retained parts is0.000967754 units. All original body/Poro/reference coordinates, weights, topology, UVs, corner normals, material slots and transforms remain exact.
- `validate_shield_reduction_targeted.py` reloads the saved trial and compares17 evaluated poses: protected body coordinates exactly equal the parent, shield follows native Shield transform within0.00001, and all coordinates are finite. Four gameplay previews (idle17, run-E14, R17, recall65) were visually reviewed without obvious silhouette regression. Export passes: **46,765 vertices /51,253 triangles /97 joints**, exact native records and Poro. Reports: `shield_collapse_trial.json`, `shield_collapse_targeted.json`, `model_shield_collapse_trial_export.json`. The trial is **not a new accepted checkpoint**; UV interpolation, normal fidelity, aperture/contact review and full animation checks remain before promotion. Remaining total geometry is still well above the15,000–25,000 body+shield target.

Computer Use initialized and inventoried apps; launching Blender's UI timed out at app approval. No GUI scene edits occurred; visual review used the reproducible Blender renders. Python syntax checks and `git diff --check` were run for this continuation.

**Precise next implementation step:** continue Stage4 from the preserved gameplay checkpoint and the unaccepted shield-collapse trial: verify the reduced shield's viewport boundaries, normals and UV interpolation using close-up front/back and directional-E contact views; retain or locally back off visible regressions, then validate the chosen reduction across all actions before promotion. Proceed into protected-region-aware body reduction and controlled atlas baking. Do not spend another pass on invisible cuff intersections. Model-only integration/runtime remains ahead of electrical VFX, authentic SFX and VO.

### Latest continuation — support topology tests, 2026-09-13

**No new accepted checkpoint.** The torso parent remains byte-identical at SHA-256 `2501fd57112bb324515e56dc33cb986905f0e50813f5aa946c56ad27e24f030a`; accepted torso work and all 67 seam corrections stay closed. This entry supersedes the proposed broad support-loop experiment in the previous continuation: that experiment has now been implemented and rejected.

- Implemented `work/scripts/cuff_support_topology.py` and `trial_cuff_support.py`: conforming local forearm subdivision with 508 added vertices / 1,016 added triangles, retaining every original rest vertex. Object009 changes from 968 vertices / 1,758 triangles to 1,476 / 2,774. Original triangle identities, per-corner barycentric coordinates and vertex ancestry are explicit in each trial JSON. UVs, materials, normals and sharp edges are carried forward. Only cuff/distal-forearm attachment weights are candidates for change; torso, fingers in other shells, shield, other meshes, rig and animations are not edited. All scenes have diagnostic names and are **not accepted checkpoints**.
- Implemented `validate_cuff_support.py` for topology-aware comparison. It verifies exact protected mesh/weight data, original vertex positions, mapped rest surfaces/UVs, material assignments, finite normalized weights with at most four influences, and normal correspondence. It maps collision pairs to source triangles, checks nonadjacent overlaps within a source triangle, and measures dimensionless principal stretch to avoid treating shorter subdivided edges as a false improvement. The existing validator was not relaxed or changed. A same-scene identity control passed all 17 poses with **zero new pairs**.
- Tested **17 poses**: the established 15 plus R16 and R18. `cuff_support_v2_trial` (smooth attachment) fails all 17, with 1,617 directed source-pair row occurrences. R17 local maximum principal stretch improves for Object004 cuff/glove 29.45→11.97 but worsens for Object009 forearm 9.03→13.79. `cuff_support_linear_trial` also fails all 17 (1,508 rows), with forearm stretch rising to22.91. Both introduce self, cuff/skin and shield intersections. These counts are directed diagnostic rows, not unique physical collisions.
- `cuff_support_control` retains original authored weights and only subdivides/interpolates. It also fails all 17 (537 new mapped pair rows; R17 forearm stretch9.03→9.31). This establishes that interpolating new skinned vertices changes the animated surface even when the rest surface is unchanged. Adding more support triangles alone is not a solution. No intra-source folds were reported in the R17 smooth trial; cross-triangle self-intersections remain real regressions.
- Protected geometry/weight, UV/material/normal mapping and finite/four-influence contracts passed for all three completed trials. R17 and recall65 renders of all three were inspected. No full 3,963-frame animation validation or export was spent on these rejected candidates. Python compilation and `git diff --check` passed. Existing accepted/source files were not rewritten.
- `clash_braum_cuff_support_trial.blend` is the first, superseded **unvalidated generator draft**; v2 corrected preservation of original tiny weights and sharp-edge flags before acceptance testing. Keep it as diagnostic evidence, not a recovery checkpoint. The v2/control/linear `.blend` files and JSON mappings allow exact investigation of rejected geometry. `validation/cuff_support_review.json` summarizes results and exact recurring source triangles, vertices, rest positions, weights and failing poses; regenerate it with `inspect_cuff_support_failures.py`.

**Remaining blocker:** broad smooth or linear cuff/forearm attachment changes still trade cuff stretch for forearm deformation and collisions; subdivision alone also changes the posed surface enough to introduce intersections. There is no passing local topology/weight candidate.

**Next concrete action:** use the mapped cuff/skin contact patch instead of repeating broad forearm subdivision. Begin with original Object009 polygon1221 against Object004 polygons2448/2449 (recurring in seven/five poses in the smooth trial), then the corresponding opposite cuff contact Object009 polygon1325 against Object004 polygon98. The exact source vertices and pose lists are in `cuff_support_review.json`. Design a smaller cuff-rim attachment/clearance correction against the unchanged forearm parent, retaining its original posed surface; use the rejected wrist-attached trial only as a visual shape reference. Any new topology needs the same explicit source mapping and strict contact tests. Do not promote a rejected trial, rerun these completed controls, reopen torso work or start later stages.

### Current continuation — cuff/forearm, 2026-09-13

**Current priority: finish the cuff/forearm transition. Accepted torso work is closed under the user's latest instruction; preserve all 67 seam corrections and do not resume the historical outline/grip experiments.** The accepted checkpoint remains `work/scenes/clash_braum_torso.blend`, SHA-256 `2501fd57112bb324515e56dc33cb986905f0e50813f5aa946c56ad27e24f030a`. No new checkpoint was accepted this session. Its existing 59-action / 3,963-frame structural and export evidence remains valid; no rejected cuff trial replaces it.

Reconciled newer work: the September 12 cuff transition/surface/full-ring scripts and renders postdate the prior plan entry. The full-ring surface trial had no completed recorded targeted check; that check is now complete. Git is present with initial commit `5e68d90`; historical statements that this is not a repository are obsolete. The supplied shield source and numerous implementation files were already dirty on entry; they were preserved. Blender was not open. The actual saved scene was inspected through Blender's Python API, including cuff rest coordinates, weights, evaluated native poses and existing/current renders.

**Diagnosis:** the problem combines attachment weights, native forearm extension and clearance/topology. At R17, the native left elbow-to-hand distance is about 1.11 Blender units. Parent cuff edge2696–3353 grows from 0.06197 to 0.35218 (5.68x). Full-ring skin transfer moves the worst cuff edge to2671–2673, still 4.12x its rest length. Some proximal cuff vertices carry small finger influences (e.g.3105 has Thumb1 0.00335); excluding every vertex with any finger influence leaves spikes. The cap trial preserves those finger weights while transferring only elbow/twist influence. No skeleton or animation modifications were made.

| Tested candidate | Result | Poses with new surface pairs / 15 |
|---|---|---|
| Existing `cuff_surface_ring_candidates.json:surface` | Rejected: R17 cuff stretch persists; new intersections. | 14 |
| `cuff_wrist_candidates.json:hand_0.97` | Rejected: incomplete cuff selection leaves spikes and clipping. | 15 |
| `cuff_wrist_candidates.json:cap_0.03` | Rejected: retained finger weights remove spikes, but cuff intersects forearm. | 15 |
| `cuff_envelope_candidates.json:envelope` | Rejected: outward radial multi-pose fit requires up to0.09453 rest displacement, distorts cuff silhouette and adds intersections. | 15 |
| `cuff_distal_skin_candidates.json:distal_0.82` | Rejected: cuff looks attached in R17, but moving distal skin with it transfers deformation into bare forearm. Object009 R17 max edge elongation rises0.48781→0.75662; recall65 rises0.20010→0.29054. New intersections remain. | 15 |

All five completed the unchanged established15-pose surface checker, including R3/15/17/26, recall60/65/70, dance45/50/55, idle, run and directional E. Protected/unlisted coordinates and weights, topology and UV checks passed. **Surface acceptance failed**; process exit0 only means the diagnostic completed. R17 and recall65 matched local renders were inspected for each new tested candidate. These are local checks against body/shield surfaces, not whole-body or runtime certificates. No full3,963-frame candidate validation/export was run because none passed the prerequisite gates. Several alternatives exist in candidate JSONs but are explicitly unvalidated, not accepted or claimed rejected by testing.

Evidence: `validation/cuff_continuation_review.json` summarizes every completed check, failures, original checkpoint hash and unvalidated variants. `cuff_attachment_inspection.json` contains actual cuff coordinates/weights and evaluated edge/bone measurements; `cuff_wrist_inspection.json` records the first wrist candidate. New reproducible scripts: `inspect_cuff_attachment.py`, `trial_cuff_wrist.py`, `trial_cuff_envelope.py`, `trial_cuff_distal_skin.py`, `finalize_cuff_review.py`. Candidate data and renders are diagnostic only; no new `.blend` was saved. Existing torso, source assets and rejected checkpoints remain intact. Validators were not weakened.

**Exact remaining blocker:** no tested local attachment distribution simultaneously preserves cuff shape, forearm contour and surface clearance through native extension. Matching the skin stretches the cuff; attaching to the wrist exposes clearance mismatch; moving underlying skin with the cuff concentrates stretch proximally.

**Next concrete implementation action:** investigate a small topology/attachment change to the cuff and distal forearm, starting from the untouched torso checkpoint and mapped cuff edges2671–2673/2696–3353 (left) and895–986/993–1001 (right). Add only the support loops needed to distribute the transition continuously, preserving the cuff's inner/outer offsets and finger weights. Establish explicit old/new polygon correspondence before collision comparisons; raw polygon IDs cannot compare a changed topology. Use R17/recall65 and the recorded15-pose failures first, including bare-forearm contour checks, then expand coverage only after a candidate clears those gates. This is an investigation direction, not a proven solution. Do not rerun rejected weighting sweeps, reopen accepted torso work, or begin later stages.

### Current scope override — 2026-09-12 continuation

The user's current scope is **playable first**: finish model/rig, geometry and texture work, export/package a model-only build, and establish an accepted actual-runtime checkpoint before dedicated audiovisual work. Preserve that playable checkpoint independently. Final completion requires all four deliverables: Model Work, electrical Visual Effects, authentic Clash Sound Effects, and authentic Clash Voice Over. Earlier optional-VO / audiovisual-first-release wording below is historical and superseded.

During the later audio phase, inspect the user's local Rainbow Six Siege installation read-only for confidently identified authentic electricity/gadget SFX and English Clash dialogue. Use ordinary legitimate tooling only; do not bypass encryption, DRM, authentication or other access controls. If reliable extraction is unavailable, record the exact clips the user must supply. No AI imitation dialogue. Finish with whole-skin event/runtime regression while retaining the model-only rollback checkpoint.

Immediate continuation result: the unsaved `torso_local_alternatives.json:outline_s0.5` candidate is **REJECTED** against `work/scenes/clash_braum_torso.blend`. The interrupted full structural validation and candidate development export **both completed** (`torso_outline_validation.json`, `model_torso_outline_export.json`), but the subsequent full local surface comparison found 58 new pair/frame occurrences across 33 frames. No candidate scene was saved or promoted. See the newest continuation entry for evidence and exact next action.

The original 310-line handoff was read completely before changes. Its supplied file ends after the package validation paragraph; the referenced numbered stages and footnote definitions are absent. The ordered stages below make the documented dependencies executable without revisiting the established design. Historical Phase 0 statements below remain the audit baseline; this tracker records subsequent outputs.

| Stage | Status | Work / exit gate |
|---|---|---|
| 0. Source audit | Complete and re-verified | All 1,849 original hashes and 54 extracted Clash members match. See `validation/source_integrity.json`. |
| 1. Export foundation | Offline checks complete | Vanilla round-trip, 11 export-contract regressions, byte-identical skin0 BIN round-trip, and cslol-go backend fixture import/export/reimport passed. Runtime acceptance remains a separate gate. |
| 2. Source preparation | In progress | All 11 OBJ groups/material assignments reconstructed; two-face discrepancy resolved; shield transforms baked and finite UVs rebuilt. Viewport classification/open-aperture decision is complete. Finished source-material response and replacement shield materials remain. |
| 3. Proportion and rig work | Offline gameplay baseline accepted with documented limitations | `work/scenes/clash_braum_gameplay_accepted.blend` preserves the torso checkpoint exactly. Cuff gameplay gate closed after17-pose/two-view review and fresh3,963-frame validation/export. Runtime and final whole-skin presentation remain separate gates. |
| 4. Geometry and texture finish | In progress | Shield-collapse trial removes3,194 triangles;17-pose protected-body/rigid-shield checks and export pass, but trial not promoted. Continue shield UV/normal/contact review, body reduction and controlled atlases. |
| 5. Model-only build | Development export validated; actual model-only package pending stages 3–4 | Draft pair has 50,002 exported vertices / 54,447 triangles, 51 palette entries, exact native joint records and retained Poro. No atlas/BIN material integration or installable Clash build yet. |
| 6. Animation and runtime gate | Pending verified baseline and completed model | All documented animation families, directional E, culling, Poro reveal and materials. Live main WAD now exists but differs in SHA-256 from the Downloads provenance archive; patch compatibility/pristine status remains unverified. |
| 7. Electrical VFX | Pending stage 6 | Clone only changed definitions into skin0; isolated custom texture and particle geometry paths; preserve state/timing/readability. |
| 8. Selected SFX | Pending model/VFX gates | Audition and select media; version-145 bank no-op and compatible WEM replacement tests. VO remains optional. |
| 9. Release package | Pending all gates; backend format compatibility tested | Only used replacements in `Braum_Clash.fantome`; closure/hash/CRC tests and final manager import/reimport. The vanilla backend fixture passed but is not the Clash release. |

Repository discovery: this folder has no Git repository or applicable `AGENTS.md`; no `work/`, `build/`, `validation/` or `dist/` existed on arrival. Blender 5.2 is installed. The provided audit scripts are evidence generators with top-level side effects, so new implementation helpers live under `work/scripts/` instead of importing or re-running those scripts and overwriting the baseline inventory.

Reproduction begins with `work/scripts/bootstrap.py` and `work/scripts/roundtrip.py`. Machine-readable validation reports are written under `validation/`; working scenes under `work/scenes/`. No release artifact is claimed by an offline export.

### Verified implementation discoveries

- **Exporter correction:** the unmodified Aventurine vanilla round-trip regenerated inverse-bind components with a maximum difference of `0.0027103424` game units and discarded original normals (maximum component difference `1.5527793`). This failed the planned preservation gate. `work/scripts/native_export.py` retains Aventurine's SKN packing/palette code, collects corner normals with inverse-transpose transforms and corrected handedness, and preserves original SKL joint records. If the palette changes, only palette header fields/resource size change and a new palette table is appended. With an unchanged palette, the SKL is byte-identical to S0. Bone names/order/parents/rest frames and identity armature transform are checked before export. No vendor files or original assets were edited.
- **Corrected round-trip:** max position error `0.0000152588`, UV error `0`, weight error `0.0000001192`, normal error `0.0000002384`; bind error `0`; identical 76-entry palette. Exact float normals are retained for immutable vanilla/Poro geometry because Blender's compressed custom-normal storage slightly perturbs a few source corners. New geometry uses its actual corner normals and splits at hard edges.
- **OBJ discrepancy resolved:** lines 104661–104662 in group `Object007` repeat position index 19005 inside each triangle. Both triangles have zero area. The reproducible group importer omits exactly those two faces, yielding the previously observed 41,173 triangles. See `validation/body_source_preparation.json` for original statements and all group assignments.
- **Additional missing texture:** the `eye` material references an absent `Textures/CHR_Clash_Eyes_DiffuseMap.dds`. The supplied `.tga` is a documented preview substitute requiring visual review. The misleading `hair_front (1)` material/group is around the eyes, not scalp hair; do not delete it just because the target is bald.
- **Source materials:** use exact `matName` relationships from the tbscene evidence. Color textures, base normal maps, source channel assignments and full source blocks are retained in the preparation report. Principled preview response is an approximation; the custom detail-normal shader, source reflectance and atlas bake remain unfinished.
- **Shield sanitation:** all seven source meshes retain their world-space coordinates, with identity object transforms and one fresh finite UV set each. Original missing textures and unused packed hair images are excluded from the working scene. The source shield remains untouched.
- **Viewport geometry:** front/back/oblique renders and bidirectional ray tests confirm open areas at the central viewport and beside the upper bars; hardware and lower armor control rays hit geometry. The initial model uses these existing apertures. No opaque plane was added. `mesh_22.001` is a small hardware display and `mesh_23.001` a small hardware surface, not a full ballistic window. See `validation/shield_aperture.json` and `validation/shield_components.json`.
- **Draft rig validation:** all 59 native ANMs load into `work/scenes/clash_braum_animation_review.blend`; first/middle/last samples have finite mesh coordinates. Eight representative renders cover idle, attack, run, recall, Q/W/E/R. The first collar test borrowed clavicle/shoulder-helper weights and visibly stretched; the upper collar now follows Spine3. The Shield prop was repositioned from a bounding-box fit to measured idle/E/run hand landmarks without changing any joint. Further grip/finger, wrist, knee and equipment refinement remains mandatory.
- **Matched draft pair:** `build/model_draft/braum_clash.skn` and `.skl` are development outputs only. The palette differs (51 versus the original 76 entries) and has been independently verified against every positive vertex weight. All 97 joint records match the source exactly. Poro retains 769 vertices / 1,018 triangles; corner position/normal/weight differences are below `0.000002`, UVs identical. Do not install this pair by itself: the source-material submesh names remain temporary, atlases are unbaked, and `skin0.bin` has not been repathed.
- **Manager resolved by the user:** `C:\Users\etqdo\Downloads\cslol-go`, containing `ModLoader.exe` and bundled `cslol-tools/ritobin_cli.exe` / `mod-tools.exe`. `ModLoader.dll` reports file version 2.15.0 and product build `1.0.0+e5983b3964e726a7baaecfc9d1649f8a3fd95bb1`. Executable hashes are recorded in `validation/manager_compatibility.json`. The [project README](https://github.com/Aurecueil/Cs-lol-go) identifies this as cslol-go and its ModLoader entry point (accessed 2026-09-10); compatibility claims here come from the supplied binaries, not from assuming cslol-manager and cslol-go are identical.
- **Backend compatibility passed:** a clearly labeled vanilla fixture under `build/manager_compat/` imports, exports and reimports with the user's `mod-tools.exe`. The reexport is a conventional packed WAD in a ZIP; independent wadtools extraction confirms both exact payloads and expected WAD hashes. Both ZIP CRCs and case-insensitive path uniqueness pass. No overlay was started, no game files were modified, and the manager GUI was not used to install a skin. GUI import and actual League acceptance remain open gates.
- **BIN writer established:** the user's RitoBin converts the complete skin0 BIN to hashed text and back with byte-identical SHA-256 `3f9b4f8c3b621db3354b059ea037d1b065bb83feb5fc9392480d2e028436c576`. Use this binary and hashed format for subsequent lossless whole-entry edits; preserve all unrelated records.

### Stage 3 continuation — 2026-09-10

The complete existing plan, pipeline README, saved scenes, exporter and relevant validation records were checked. This is still not a Git repository, and no applicable `AGENTS.md` was found. Source audit, import reconstruction and manager compatibility work were not restarted.

- **Implemented:** `work/scripts/refine_rig.py` reads the previous animation review checkpoint and writes a separate `work/scenes/clash_braum_rig_refined.blend`. It does not regenerate or overwrite that input scene. The draft left fingers had no finger-joint weights, while several right fingers inherited index-finger weights. A measured, orientation-preserving hand fit and explicit digit-shell assignments now correct 2,076 hand/cuff vertices. Source physical shell IDs and fit residuals are retained in `validation/rig_regions.json` and `validation/rig_refinement.json`.
- **Equipment and cloth:** 78 deliberately selected equipment/strap/armor shells plus 22 nearby fasteners (100 components / 8,352 vertices) now follow stable native bones. Nine continuous cloth/neck shells receive local weight smoothing across coincident position seams, preserving geometry, UVs, head rigidity and digit assignments. The lower neck no longer borrows jaw/clavicle motion. No joints, native animations, gameplay records, shield geometry or Poro geometry were changed.
- **Development export:** `build/model_rig_refined/braum_clash.skn` and `.skl` are the current refinement pair: 50,002 exported vertices, 54,447 triangles including Poro, and a 64-entry palette. Exact native joint records, rigid Shield weights, normalized maximum-four influences and Poro corner preservation pass the existing independent export checks. This is still above the triangle target and has temporary material names; do not install it as a finished skin. `export_draft.py` now accepts explicit scene/output arguments; its original defaults remain available.
- **Important new review evidence:** first/middle/last samples missed severe intermediate deformation. The new validator samples all 3,963 integer frames across the 59 native clips and compares actual evaluated mesh data, rigid-component transforms and unchanged imported action channels. `braum_spell4` frame 3 also stretches vanilla Braum under the same action; the source ANM contains unusually large joint translations there (see `validation/animation_extremes.json` and `validation/previews/vanilla_extremes/`). Preserve this source motion. Do not classify every large edge ratio as a Clash-specific failure or modify ANMs to lower the score.
- **Visual status:** hand close-ups show the draft left wrist collapse corrected and right digits curling together in E. Recall/dance hardware spikes were traced to individual components and corrected. Stage 3 is **not final**: inspect forearm/cuff continuity and armpit/shoulder attachment at recall frame 65, the seat/crotch and torso overlap at dance-loop frame 50, and palm/handle contact through E angles and R. These are concrete remaining art checks before reduction/baking. Render manifests identify the scene hash and exact cases; a numerical pass does not establish collision-free poses or runtime snapping.
- **Final checks for this checkpoint passed:** all 3,963 frames have finite evaluated geometry and valid authored weights; all ten exposed digit shells address their intended native digit chains; imported action channels and native rest contract are unchanged. Maximum error between 100 rigid components and their intended native bone transforms is `0.0000023383` Blender units. Current export preserves Poro with maximum corner-position error `0.0000019074` game units and identical UVs. Python compilation passes. The final 27-render set and labeled contact sheets are in `validation/previews/`; [hand comparison](../validation/previews/rig_hand_comparison.png) shows before/after with matching cameras. Detailed results: `validation/rig_refinement_validation.json`, `validation/model_rig_refined_export.json`, `validation/refined_render_manifest.json`. No atlas, skin0 integration, model-only install or release package has been produced.
- **Corrected external checkpoint:** the live `C:/Riot Games/League of Legends/Game/DATA/FINAL/Champions/Braum.wad.client` now exists (92,800,510 bytes). SHA-256 is `fa02c0a98b5080f3524d8d6daafc4bf3019b83c60d5a42b64bd88cd9bf722414`, versus `e2b651ef2a582c23331d848019949079a9235ed6d1a4c8c21da4fb55c361627d` for the Downloads archive. This session only read/hashed it. Presence and equal file size do not establish patch compatibility or identical member content. See `validation/live_wad_checkpoint.json`; perform provenance/content verification at the runtime baseline gate instead of assuming it is still missing.

The original `clash_braum_animation_review.blend`, `clash_braum_fit_draft.blend` and `build/model_draft/` remain earlier checkpoints. Use the refined scene and pair for continuation. The [working pipeline instructions](../work/README.md) include the new commands and diagnostic tools.

### Stage 3 deformation continuation — 2026-09-11 UTC

**Current scene: `work/scenes/clash_braum_deformation.blend`.** This is a separate child of `clash_braum_rig_refined.blend`; all previous scenes and source assets remain intact. Git now exists, with baseline `5e68d90`; the root AGENTS.md was read. Earlier no-Git/no-AGENTS statements describe historical sessions.

- **Implemented:** `work/scripts/refine_deformation.py` changes weights only on 898 vertices: 64 vertices in each skin forearm (Object009 shells 652/810), 561 seat vertices (Object001 shell 0), and 209 belt vertices (Object006 shell 1366). Forearms use an axial elbow-to-hand transition instead of the prior fixed x>.71 smoothing boundary. Seat upper-spine influence moves to Root, fading over rest z=1.04..1.14 and retaining hip/knee contributions. The belt follows Root alongside its existing rigid hardware. No geometry, topology, UV, material, digit-shell, Shield, Poro, rest-bone or action changes. Reproduction/report: `refine_deformation.py`, `validation/deformation_refinement.json`.
- **Cheapest diagnostic first:** `inspect_cloth_defects.py -- --scene clash_braum_deformation.blend --output deformation_cloth_defects.json`. Dance frame 50 worst seat absolute edge elongation drops from 0.215145 to 0.075188 Blender units; the conspicuous projecting seat flap is gone in the inspected render. Belt shell 1366 no longer appears among elongated edges. Recall skin worst elongation decreases from 0.215653 to 0.200096, but the worst edge moves to the right arm (666–674); cuff/shoulder art approval is still open. Reports retain exact indices, rest points and weights.
- **Full validation passed:** `validate_rig_refinement.py -- --scene clash_braum_deformation.blend --refinement deformation_refinement.json --report deformation_validation.json --reuse-before`: 3,963 integer frames, 59 clips, finite evaluated geometry, normalized maximum-four weights, ten native digit assignments, unchanged native rest contract/actions. All 101 rigid components track their bones with max error 0.00000233825 Blender units. Object006 maximum all-frame edge ratio improves 97.6822→22.0990. Object009 maximum improves 29.6591→14.5442, while maximum frame p99 slightly increases 12.7334→12.9005; these are diagnostics, not acceptance thresholds. The native R-frame-3 extreme remains unchanged.
- **Export passed:** `export_draft.py -- --scene clash_braum_deformation.blend --output model_deformation` writes the new development pair. 50,002 vertices / 54,447 triangles / 64 palette entries, exact 97 joint records, rigid Shield, Poro position error 0.00000190735 game units and identical UVs. See `validation/model_deformation_export.json`. Old export pairs remain earlier checkpoints; no installable skin was made.
- **Visual evidence:** inspected two whole-body recall/dance renders (`deformation_render_manifest.json`) and four E±180/R15/R26 close-ups, then four isolated reverse-angle close-ups (`deformation_grip_isolated_render_manifest.json`). The ordinary E camera was occluded by the torso. `render_rig_refinement.py` now supports `--isolate-grip` and honors `--direction` for hands; this hides torso/equipment for diagnostic renders only, never saves scene changes. Isolated images are not whole-body collision approval. Previews are ignored/reproducible; the JSON manifests record scene hashes and cases.
- **Grip blocker/evidence:** isolated E±180 frame 29 right palms remain beside the shield bars rather than demonstrably enclosing a handle. R15 left wrist lies by the shield plate with an open palm; R26 left hand has no shield in the close-up. `inspect_grip_targets.py` records both native palm landmarks in Shield bind space at 13 poses (all idle/run E directions, R3/15/26) in `validation/grip_targets.json`. These are joint-derived targets, not palm-surface contact measurements. R15→R26 left target changes from (-1.40868,-0.09724,0.23385) to (-1.10787,-0.61994,-0.77102). A fixed shield/handle translation cannot be assumed to fix both. Resolving intended release/snap behavior needs comparison against native geometry and the existing graph/snap contract before reshaping handles; no native animation was altered or guessed constraint installed.
- **Remaining:** shoulder/torso overlap, forearm/cuff silhouette continuity, residual seat/hip deformation and palm/handle fit. Remaining diagnostic examples: recall Object002 edges 1497–2321 (rest 0.00333→posed 0.10675), Object004 1702–1770; dance Object007 shells 2583/2611 and Object006 shell 1400 still have mixed attachment spikes. These compact components can be addressed independently of the harder multi-pose grip problem.
- **Exact next action:** open `clash_braum_deformation.blend`, isolate Object002 shell 0 and Object004 sleeve shells at recall 65, and correct cross-garment shoulder weights using the listed short-edge discontinuities; compare the same isolated camera before/after. Then inspect the listed compact torso/belt components and their neighboring attachment bones. Use the new grip targets for a native-versus-Clash E/R comparison before changing handle placement. Do not rerun prior import/refinement stages or advance to reduction/baking yet.

### Stage 3 shoulder continuation — 2026-09-11 UTC

**Current scene: `work/scenes/clash_braum_shoulders.blend`.** Its input `clash_braum_deformation.blend` is preserved. Reproduce only this child with `work/scripts/refine_shoulders.py`; do not restart earlier stages.

- **Implemented shoulder correction:** a shared, rest-position-welded Gaussian weight field for Object002 shell 0 and Object004 upper sleeves 12/253, radius 0.075, sigma 0.03, two passes, 50% correction strength with smooth spatial boundaries. Changed 905 vest and 320 upper-sleeve vertices. The active region starts at z=1.47, reaches full mask at 1.53, fades above 1.65..1.73 and outside |x|=.30...40; centerline fades at |x|=.045...10. Weights remain normalized top-four. This is a partial shoulder improvement, not final art approval.
- **Independent torso progress:** Object006 shell 1400 (60 vertices) follows Root; Object007 shells 2583/2611 (36/12 vertices) follow Spine2 at their rest height. These remove the previously documented compact mixed-attachment spikes. The 101 previous rigid components, including the corrected belt 1366, remain unchanged; total is now 104.
- **Targeted validation:** `validate_shoulders_targeted.py` compares the parent and child at 12 poses (recall 60/65/70, dance 45/50/55, R3/15/26, idle17, E180/29, run12). It asserts exact rest geometry and all weights outside the explicit changed-index lists are unchanged, including every previous skin-forearm, seat, belt and digit correction. Report: `validation/shoulder_targeted_validation.json`. Recall edge 1497–2321 changes from 0.106753 to 0.053515 posed length (rest 0.003327); vest maximum absolute elongation falls 0.103425→0.072945. Dance Object007 maximum elongation falls 0.157925→0.033578; Object006 falls 0.133885→0.120849. Sleeve maximum recall elongation is unchanged at 0.106274: its distal elbow/cuff issue was deliberately not modified by this upper-shoulder pass.
- **Iteration limits/tradeoffs:** a broad torso/elbow smoothing attempt increased dance stretch and was discarded. A narrow kernel transferred the shoulder discontinuity to adjacent longer edges; the final wider kernel is restricted to the upper shoulder and reduced to half strength. Do not repeat unrestricted smoothing. Final local maximum elongation increases remain in E180/29 (Object002 0.026896→0.039344) and R3 (0.049014→0.081983), although R3 p99 improves 0.02457→0.02383 and E p99 is essentially unchanged. Matching isolated renders were reviewed for recall, idle, E180 and R3; no final collision-free claim is made. Further elimination of the shoulder crease requires coordinated topology/weight work across poses rather than increasing smoothing strength.
- **Comprehensive checkpoint:** full validator run once after the final candidate: `validate_rig_refinement.py -- --scene clash_braum_shoulders.blend --refinement shoulder_refinement.json --report shoulder_validation.json --reuse-before`. All 3,963 frames / 59 actions pass finite geometry, normalized maximum-four weights, ten digit assignments, unchanged native rest/action contracts and rigid-component transforms (max error 0.00000233825). Object002 all-frame max edge ratio 32.0820→25.7259; max frame p99 4.94637→4.73714. Object007 max ratio 43.2720→37.35797, p99 7.08946→6.07588. Object004 global metrics unchanged; Object006 p99 improves 2.79422→2.58583. Worst vest ratio now occurs at R17; this is not equivalent to new damage at R17.
- **Export:** `export_draft.py -- --scene clash_braum_shoulders.blend --output model_shoulders` passed. New development pair under `build/model_shoulders/`, with 50,002 vertices / 54,447 triangles / 64 palette entries, exact 97 joint records, rigid Shield and retained Poro (position error 0.00000190735, UV error 0). Report `validation/model_shoulders_export.json`. No material/atlas/runtime release gate is closed.
- **Render/diagnostic tooling:** `render_rig_refinement.py` now supports repeatable `--only-object` for temporary object isolation. The matching before/after manifests are `shoulder_before_render_manifest.json` and `shoulder_after_render_manifest.json`; whole-body recall65/dance50 are in `shoulder_body_render_manifest.json`. Ten final renders were inspected. `inspect_cloth_defects.py` now accepts repeatable `--case clip:frame`; defaults remain recall65/dance50. Final reports: `shoulder_cloth_defects.json`, `shoulder_r17_defects.json`. At R17 the longest absolute vest elongations are on unchanged lower-torso edges 143–190, 1102–1103, 133–152 and 128–138. Inspect those alongside dance50 for the remaining torso overlap; do not conflate the short-edge ratio maximum with these absolute maxima.
- **Native grip comparison implemented:** `inspect_native_grip.py` writes `validation/native_grip_comparison.json` for the 13 previously recorded E/R poses, comparing native Shield-weighted faces and actual CCE geometry at identical joint-derived palm midpoints. R26 left distance to shield is 1.0174 native / 1.0409 Clash; right is 1.3646 / 1.2892. This establishes separation already in the offline native pose, so forcing R26 contact is not justified by that pose alone. Running E14 left distance is 0.0236–0.0246 native versus 0.2460–0.2465 Clash; right is about 0.031 versus 0.059. Idle E left is separated on both; right is 0.031 versus 0.057. These distances are to any shield surface, not measured handles or actual palm surfaces; runtime snapping remains unmodeled. The running-E left side is now the concrete highest-priority grip discrepancy.
- **Exact continuation:** use `clash_braum_shoulders.blend`. First inspect the unchanged lower-torso edges above in R17 and dance50 using targeted `--case` diagnostics and matching isolated Object002/Object004 renders, with the previous seat/belt corrections protected. Remaining recall forearm/cuff issue includes Object004 edge 1702–1770 and glove shell 2539; do not resmooth the protected Object009 forearms. For the next independent grip change, isolate **L_Hand in braum_spell3_run0 frame 14** with native/Clash matching cameras, identify the CCE handle region relative to the measured native proximity, and fit geometry only after checking idle/right-hand and R15 constraints. Do not force R26 grip. Shoulder/torso/cuff and grip art gates remain open.

### Stage 3 lower-torso continuation — 2026-09-11 UTC

**Current partial-improvement checkpoint: `work/scenes/clash_braum_torso.blend`.** Parent `clash_braum_shoulders.blend` remains byte-identical. The remaining competing art measurements below are the requested **Medium escalation point**, not grounds for repeated Low smoothing experiments.

- **Implemented:** `refine_torso.py` averages the original weights within 29 connected short-edge clusters (edges <0.008, rest z strictly 1.30..1.36) in Object002 shell 0, then normalizes the largest four influences. Exactly 67 vest vertices change. No geometry, UVs, topology, joints or animations change. All other weights, including previous shoulder, sleeve, forearm, hand/digits, seat, belt and 104 rigid attachments, are exactly preserved. `validation/torso_refinement.json` contains every changed index and cluster.
- **Diagnostic discovery:** R17's previous maximum vest ratio was edge 1094–1095 (rest positions around x=.123,y=.113,z=1.333): a 0.0243 L_Shoulder influence and differing Spine1/2/3 weights separated nearly coincident vertices. Other short discontinuities: 733–737, 148–150, 157–158, 1096–1097, 145–146. This differs from the previously listed longest absolute edges. Restricting corrections to these physical clusters removes sharp crease defects without replacing the lower torso's whole weight field.
- **Targeted evidence:** `validate_torso_targeted.py` checks 15 poses: recall60/65/70, dance45/50/55, R3/15/17/26, idle17, idleE180/29, run12, runningE0/-90 frame14. Exact geometry and every unlisted vertex weight match the parent. Report `validation/torso_targeted_validation.json`. At R17 edge1094–1095 length drops 0.099963→0.002645; 733–737 0.070030→0.002261; 148–150 0.101282→0.003587. Vest max ratio drops 25.7259→11.8376 at R17 and 15.0244→10.9299 at dance50. Recall65 and sampled idle/running-E maximum elongation remain unchanged; the corrected shoulder edge is unchanged in all sampled poses.
- **Explicit remaining tradeoff:** equalizing the short seams redistributes deformation to neighboring longer edges. R17 max absolute vest elongation rises 0.134079→0.168246; dance50 0.110011→0.119403. Edge143–190 posed length rises 0.180326→0.214493 at R17 and 0.141746→0.165650 at dance50. P99 absolute elongation changes R17 0.082855→0.086430, dance50 0.054611→0.055569. Matching rear isolated renders show the sharp rippled discontinuities reduced, but torso folding/overlap remains. The latest scene is a local crease improvement, not a globally collision-free solution. A coupled multi-pose/topology treatment must balance short-seam continuity and long-edge stretch; do not optimize only the ratio score.
- **Discarded attempts, do not repeat:** assigning axial adjacent native spine weights increased dance50 max elongation to0.18922 and R17 to0.19078; height-filtering broad four-spine support gave0.13079/0.15057 and worsened idle/running-E. Merely remapping all lower non-spine influences to Spine3 barely improved target maxima while worsening idle and running-E maxima by about0.019. None of those broad candidates remain in the final scene or generator. Final generator uses only the 67-vertex clusters.
- **Validation checkpoint passed:** one comprehensive run after the final local candidate: `validate_rig_refinement.py -- --scene clash_braum_torso.blend --refinement torso_refinement.json --report torso_validation.json --reuse-before`. All 3,963 frames/59 actions, normalized maximum-four weights, ten digit assignments, finite evaluated geometry, native rest/actions and 104 rigid attachments pass; max rigid error0.00000233825. Whole-animation Object002 max edge ratio improves25.7259→18.59898 (worst now R30), max frame p99 ratio4.73714→4.46448. Export to `build/model_torso/` passed:50,002 vertices/54,447 triangles/64 palette entries, exact97 native joint records, rigid Shield, retained Poro with identical UVs and position error0.00000190735. Reports `torso_validation.json`, `model_torso_export.json`; no release acceptance implied.
- **Rendered/inspected:** four matching isolated Object002 rear views (R17/dance50, parent/child), two final whole-body views, plus running-E left-hand front/reverse views and a native reverse view. The native reverse view was occluded and is not useful contact evidence. Final manifests: `torso_before_render_manifest.json`, `torso_candidate_render_manifest.json` (this label refers to the final saved local candidate), `torso_body_render_manifest.json`. `torso_cloth_defects.json` was refreshed against the final scene. The front grip view is in `running_e_grip_front_render_manifest.json`.
- **Grip implementation/trial:** refreshed `inspect_grip_targets.py -- --scene clash_braum_torso.blend`. Extended `inspect_native_grip.py` with `--scene`, `--output` and `--trial-left-offset`; the trial changes CCE geometry in memory only and never saves a scene. A computed bind-space translation (+0.192218,-0.110767,+0.000938) brings running-E0/14 left-palm landmark proximity0.2464→0.0245 (all running E directions0.0242..0.0247). However, running-E right proximity worsens~0.059→0.1932..0.1936, idle-E right~0.057→0.1951..0.1952; R15 right0.0338→0.2017 and left0.0803→0.1338. **Rejected, no shield/hand change saved.** Evidence: `validation/grip_offset_trial.json` against baseline `native_grip_comparison.json`; actual rest CCE geometry remains unchanged. Distances are joint-midpoint to any shield surface, not handle-specific/palm contact tests. Front E0/14 view confirms the palm is off the shield; moving the whole shield is not an acceptable correction. R26 native release/separation evidence still applies.
- **Next exact continuation (Medium recommended):** start from `clash_braum_torso.blend`, retain parent for comparison. For lower torso, jointly inspect Object002 clusters1094–1095/733–737 and longer edge143–190 at R17/dance50; preserve all other authored regions and evaluate both ratio and absolute length. For grip, isolate left palm and the CCE handle geometry at `braum_spell3_run0:14:L_Hand` with `--isolate-grip --direction=3,-7,3`; fit the local handle/hand region with idle-E right and R15 both-hands constraints, rather than a global shield offset. Use the 13-pose reports as measured competing targets and verify actual palm surfaces before accepting a fit. This coupling is the escalation point; no further blind Low trials are warranted. Recall cuff/elbow, torso overlap and E/R contact gates remain open.

### Stage 3 coupled torso / local handle review — 2026-09-12

**Accepted working scene remains `work/scenes/clash_braum_torso.blend`, SHA-256 `2501fd57112bb324515e56dc33cb986905f0e50813f5aa946c56ad27e24f030a`. No new art checkpoint was accepted.** The existing dirty working tree and all parent scenes were preserved. No Stage 4 or runtime gate was advanced. Review disposition: `validation/stage3_local_review.json`.

- **Bounded torso experiment:** `inspect_torso_coupled.py` compares six local endpoint/cluster blends across the existing 15 poses. Vertex143 is below the corrected seam band and has no coincident duplicate within .001; vertex190 belongs to the preserved [190,331] cluster. A 10% blend of 143 toward 190, normalized to four influences, changes only vertex143. R17 maximum elongation improves .168246→.154099 and dance50 .119403→.110011; edge143–190 lengths improve .214493→.200346 and .165650→.155919. Target R17/dance p99 and sampled idle/running-E maxima remain unchanged. All 67 seam vertices and every other weight/rest vertex are exactly preserved by the targeted comparison. Larger blends worsen neighboring strain or p99; 50% at cluster190 increases maxima to .197204 R17 / .140852 dance50.
- **Torso trial rejected visually:** `clash_braum_torso_coupled.blend` is a diagnostic child, **not the next working scene**. Matching edge-centered R17/dance50 views show a small new notch in the dance silhouette, despite better scalar metrics. Preserve the parent. Evidence: `torso_coupled_trials.json`, `torso_coupled_targeted_validation.json`, `torso_coupled_refinement.json`, `torso_edge_before_render_manifest.json`, `torso_edge_after_render_manifest.json`, `torso_coupled_candidate_render_manifest.json`. Whole-vest parent views and four edge-detail renders were inspected.
- **Local grip geometry identified:** `inspect_local_handle.py` maps 159 position-welded CCE physical shells and measures actual hand-weighted mesh vertices against their triangles. Running-E0/14 left hand is nearest **CCE_mesh_14.001 shell20 (151 vertices, outer side rail)**, then trim shell482. The minimum sampled hand-surface gap to shell20 is .133425, versus joint-midpoint gap .246351. There is no existing grip bar at that palm location. Right-hand nearest hardware is **CCE_mesh_13.001 shell940 (208 vertices)**, with sampled surface minima .000685 running E / .000286 idle E; this does not prove enclosure. R15 nearest left/right components are CCE_mesh_14.001 shells6716/3081. Exact indices/bounds/face sets: `local_handle_diagnostics.json`. Measurements include hand-weighted cuff regions and are unsigned; they are diagnostics, not palm pressure/contact certification.
- **Local geometry trials, all unaccepted:** `trial_local_handle.py` adds only `CCE_auxiliary_grip_trial` (72 vertices, 42 polygons, rigid Shield weight) to a separate child, leaving every original mesh/weight/UV exact. It tests a straight grip with two rail supports, not a whole-shield translation. Initial midpoint fit reduces running-E left joint distance to .0206 and leaves idle/right/R15/R26 competing distances unchanged, but introduces **82 glove triangle intersection pairs** at run0/14. Increasing clearance gives55; moving the center to the Index1/Pinky1 midpoint gives73. These are rejected. `validate_local_handle_targeted.py` verifies original mesh preservation and checks actual triangle intersections against every source body group in all13 E/R samples. Reports: `local_handle_initial_targeted.json`, `local_handle_surface_targeted.json`, `local_handle_knuckle_targeted.json`, `local_handle_trial_joint_comparison.json`.
- **Surface-based trial also not accepted:** two cheap, bounded 144-candidate searches examine rail-direction and anatomical-normal offsets with support-span/longitudinal variation. Reports retain collision-free candidates, not acceptance rankings. The normal-offset child `clash_braum_handle_normal_trial.blend` clears all13 sampled body intersection checks, with minimum sampled left glove/digit distance .006518 at run0/14, but front/reverse renders show an open hand beside the bar and conspicuous projecting supports. This is insufficient actual grip improvement. No native action, joint, hand weighting or original CCE geometry was changed. `local_handle_normal_targeted.json` records preservation and intersections. All `clash_braum_handle*_trial.blend` files are **diagnostic/rejected**, not accepted continuation scenes.
- **Visual tooling:** `render_rig_refinement.py` now supports `--edge Object002:143:190 --scale .4`; `--vanilla --isolate-grip` removes native torso occluders; `--hand-surfaces` additionally removes sleeves/forearms for diagnostic contact views only. It never saves these visibility/topology changes. Manifests record these options and input hashes. Inspected views include native running-E front/reverse, Clash running-E/R15 layout, first-trial competing hands, and front/reverse views of all four local grip fits. Native white preview materials still limit surface shading interpretation. `local_handle_*_render_manifest.json`, `native_grip_isolated_*_render_manifest.json` and `clash_grip_layout_reverse_render_manifest.json` locate the evidence.
- **Validation policy:** no comprehensive validation or export was spent on rejected geometry. Existing parent 59-action/3,963-frame and development-export passes remain authoritative. The original torso scene hash and restored initial trial render hashes were explicitly checked. A trial-output naming bug briefly replaced only the new initial handle trial; its exact Blender backup was restored and verified against both original manifests. No accepted/source scene was affected.
- **Exact next action:** keep the torso parent. For grip, model a contoured side grip from the **actual glove palm/finger surfaces**, routing support endpoints outside the thumb/pinky envelope; a straight bar placed from joint midpoints is inadequate. Use the identified rail20 as attachment context and preserve right hardware940 and R15 side contacts. Test the same13 poses with triangle intersection checks and matched front/reverse views before acceptance. Do not force R26 contact. The lower-torso silhouette, recall cuff/edge1702–1770/glove2539, and other documented Stage3 art gates remain open.

### Stage 3 interrupted outline recovery — 2026-09-12

**Accepted scene remains `work/scenes/clash_braum_torso.blend`, SHA-256 `2501fd57112bb324515e56dc33cb986905f0e50813f5aa946c56ad27e24f030a`.** No new scene checkpoint is accepted. Existing source assets, the 67 seam vertices, rejected trial scenes and earlier evidence remain intact.

- **Recovered completed work:** `torso_outline_validation.json` contains all 59 actions / 3,963 frames and successful final native-rest/action flags. `model_torso_outline_export.json` contains the corresponding in-memory candidate export, 50,002 vertices / 54,447 triangles / 64 palette entries, exact 97 native joint records, rigid Shield and preserved Poro. `audit_outline_checkpoint.py` verifies parent/candidate identity, all 59 source ANM hashes, exact clip/frame coverage and unchanged worst-case edge metrics versus the torso checkpoint; it records current export hashes in `torso_outline_recovery_audit.json`. Those expensive completed checks were not rerun.
- **Historical experiments recovered:** ring/positional and helper-weight variants in `torso_local_alternatives.json` remain rejected for sharper dance silhouette. Full outline strength (`outline_s1.0`) introduces four pairs at recall65, Object002 polygon316 versus Object007 polygons2886/5774/5856/5857. Half strength passed the original 15 poses and removed nine pairs at recall70, but this was insufficient temporal coverage.
- **Full local surface result — half strength rejected:** `torso_outline_half_all_frame_local.json` checks the 3,963 original integer frames both before and after, all geometry/UV/protected-weight invariants, and changed-vertex incident faces against all body/shield meshes. It found **58 new pair/frame occurrences in 33 frames**, despite removing 71 other pair/frame occurrences. Failures: attack02 frames4/6; channelwindup12/14; joke_in41/51/131/132/137; passive_attack_01 frame16 and18–29; recall66–68/71–72; spell3_punch135 frame17; taunt150–154. Shared-vertex self pairs, Vanilla_Reference and Poro are excluded. This is a local regression test, not full-body collision or runtime certification. Exact pairs are retained in the full report and recovery audit.
- **Validator improvement:** conservative vectorized polygon bounds filter remote candidates before the unchanged Blender BVH overlap test, preserving original polygon IDs and face tessellation. All original 15-pose rows exactly match (`torso_outline_half_culling_check.json`). `--all-frames` and repeatable `--case clip:frame` permit exhaustive validation and inexpensive exact-failure checks. The initial slower attempt was stopped before any scene mutation; the optimized complete run exited successfully. Numerical completion does not imply acceptance: inspect new-pair results.
- **Smallest local corrections tested:** `search_outline_backoff.py` tests only vertex143 at 0.75/0.5/0.25/0.125/0.0625 times the half-strength delta, using the 33 failure frames plus original 15 poses. Failure-frame counts are31/21/7/4/0. The last has only ~0.000105 R17 / ~0.000186 dance50 maximum-elongation improvement, has not received all-frame/export/visual acceptance, and is **not promoted**. Reports: `torso_outline_backoff_candidates.json`, `torso_outline_backoff_search.json` and individual trials. Do not treat a nearly zero adjustment as finished torso work.
- **Bounded alternative directions:** `search_outline_constraints.py` tests252 nearby Root/Spine2/Spine3 combinations plus baseline/half controls. No combination clears all48 sampled poses while improving both target edges by >0.0001 Blender units. Its LBS prediction agrees with Blender within1.49e-7 and reproduces the exact33 half-strength failure-frame pair sets; this is only a bounded search, not proof that all weighting solutions are impossible. `torso_outline_constrained_search.json` retains every candidate and its failures. No geometry/weight changes are saved.
- **Failure mapping and visual limits:** `torso_outline_failure_geometry.json` records actual vertices/rest positions/weights for all implicated polygons: Object002 shell0 faces231/316/558/559; remote Object007 shells1668/1672/1676/1680, Object004 sleeve shell12, Object009 forearm shell652. The recovery inspected matched R17/dance50/recall65/running-E14 views and new recall68/passive24 parent/candidate views (`outline_failure_*_render_manifest.json`). Surrounding equipment occludes much of the affected patch; visually similar renders cannot override the measured regression. Stage3 art/grip/cuff/torso gates remain open.
- **Exact next action (Medium):** preserve the accepted torso and all67 seam weights. Inspect the local triangles incident to143 and edge143–190 together with the mapped neighboring surfaces at recall66–72 and passive18–29. Design a small local topology/clearance correction in a separately named child; do not resume blind scalar weight backoff or broad torso reweighting. If topology changes, replace raw polygon-ID comparison with an explicit old/new surface mapping and revalidate UV/material/export contracts. The half-strength and constrained-search failures provide the regression pose set. No Stage4, audiovisual implementation, installable package or runtime test was started.

### Next implementation work from this checkpoint

1. Finish stage 3 from **`work/scenes/clash_braum_torso.blend`**, not the earlier seed. Start with the remaining forearm/cuff and shoulder continuity at `braum_recall` frame 65, pelvis/seat and torso overlap at `braum_dance_loop` frame 50, then palm/handle contact at E directional extremes and R overhead/landing. Use `inspect_cloth_defects.py` for exact remaining edges and `render_rig_refinement.py` for isolated poses. Keep the original R-frame-3 stretch; the vanilla comparison and source translations establish that it is not introduced by this hand/equipment pass. Do not mark the rig final because the all-frame numerical checks passed.
2. Reduce the current 53,429 body+shield triangles toward 15,000–25,000, retaining the separate 1,018-triangle Poro. Preserve face/fingers/outline; remove hidden or redundant source geometry before aggressive decimation. Validate exported split-vertex counts again.
3. Bake reconstructed materials to a controlled atlas, including source channel/detail interpretation. Author finished shield frame/electronics textures. Replace temporary source submesh names with `Braum` / `ShieldFrame` and retain `Poro`; add `ShieldGlass` only for a separately tested pane.
4. Use the proven RitoBin no-op workflow for exact new model/texture references in skin0, then validate model-only reference closure and package it for cslol-go. The live main WAD now exists; establish its pristine, patch-compatible content before runtime testing. Its hash differs from the supplied provenance archive.
5. Resume VFX, then selected SFX, only after the model gate. No electrical effects, replacement audio or final `Braum_Clash.fantome` has been produced yet.

See [working pipeline instructions](../work/README.md) for commands and generated-file ownership. Source preparation renders are under `validation/previews/`; draft animation renders under `validation/previews/draft/`.

## Technical decision

The conversion is feasible as a substantial mesh, material and effects project. It is **not ready for direct export**. Use base Braum, the supplied bald Clash body, and the supplied reworked CCE shield. Preserve Braum’s native joint hierarchy, bind pose, animation graph, gameplay records and event timing. Reshape and weight Clash to that system. Build a matched SKN/SKL pair, validate a model-only replacement, then add electrical effects and selected audio.

The highest-priority discoveries are:

1. **Braum’s shield is part of his character mesh, with its own animated skeletal branch.** It is not a separate shield file attached to the left hand. Exactly 1,398 vertices in the original `Braum` submesh are rigidly weighted to `Shield`. The branch is `Origin → Shield`; both hand snap bones are separate helpers.
2. **The Clash body has no usable rig.** The supplied OBJ has geometry and UV coordinates but no armature, weights or material assignments. A complete Braum-compatible weighting pass is required.
3. **The shield source is incomplete.** Three distinct active texture files are missing, a duplicate image datablock points to one of those same files, and several of its 11 UV layers contain inappropriate values, including NaNs. The transparent viewport is not an export-ready material.
4. **Preserving the skeleton does not mean blindly reusing its original bytes.** Aventurine rebuilds the mesh’s bone influence palette. A newly exported SKN must be paired with a compatible SKL palette while retaining the original joint names, hierarchy and bind transforms.
5. **Most base effects are in shared multi-skin BINs.** Only three relevant VFX definitions are embedded in `skin0.bin`. Copying or editing presumed “base particle files” would miss the actual relationships and could affect other skins.
6. **The live main Braum WAD is absent at the inspected installation path.** A separate archive in Downloads exactly reproduces the supplied extraction, but the live game needs a pristine, patch-matched archive before testing.

The recommended first release includes the Clash body, tactical equipment, rigid CCE shield, a tested translucent or open-aperture viewport, readable electrical Passive/Q/W/E/R effects, and selected electrical SFX. Full custom voice-over is an optional extension. Keep the original Poro joke component for compatibility in the first release; it is a small deliberate visual exception, not an overlooked mesh.

This document specifies future work. No replacement skin, modified game asset or `.fantome` package was created in Phase 0. All 1,849 original project files passed a final SHA-256 preservation check. Audit-only extracted copies, decoded records, inspection scripts and a geometry diagnostic image are separate from the originals.[^1]

## Evidence and confidence

**Confirmed** means directly read from the supplied files, archive records, installed binaries or inspected source code. **Recommendation** means an engineering decision based on that evidence. **Test gate** means behavior that requires a future export or in-game check; it is not represented as already working.

The local League metadata reports `16.17.8104348+branch.releases-16-17.content.release` and the corresponding `.code.public` version. This identifies the inspected installation; it does not prove the separately supplied main WAD belongs to that exact patch, nor establish the latest public patch. The main and localized archives use WAD version 3.4. Source archive provenance is partially established: all 1,843 supplied extracted files match an independent extraction of the Downloads archive, with no missing or mismatched files.[^1]

The audit resolves **1,802 of 1,852 combined WAD entries**: 1,793 of the 1,843 main entries, plus all nine localized entries. Fifty original main entries retain unknown paths. All 91 supplied BINs parse successfully and their file paths are resolved. All 72 non-null base VFX bindings resolve to definitions; their 135 unique explicit file dependencies are supplied. This is not a claim that every engine shader, shared audio object or global game dependency is contained in this champion archive.

The complete evidence accompanies this report:

- [Searchable original-file inventory](ORIGINAL_FILE_INVENTORY.html): every original filename, extension, absolute original path, likely purpose, relevance, discovered references, size and SHA-256.
- [Technical appendices](TECHNICAL_APPENDICES.md): exact shared BIN paths, all 97 joints, all 59 animation files and graph bindings, every base VFX dependency, all 94 audio events and their media relationships, and all archive members.
- [Machine-readable inventory](evidence/project_inventory.json), [BIN index](evidence/bin_index.json), [path map](evidence/path_map.json), [preservation check](evidence/preservation_check.json).

No in-game test, new asset export, material conversion or auditory identification of every media clip was performed. Shader compatibility, deformation quality, actual sound selection and manager import behavior remain explicit later gates.

## Supplied project inventory

Project root, abbreviated **P** below, is `C:\Users\etqdo\Downloads\Clash Braum project`. Original source paths are immutable inputs. Future paths beginning `work/`, `build/`, `validation/` or `dist/` are **proposed outputs**, not files claimed to exist.

| Original path relative to P | Type / count | Likely purpose and relevance |
|---|---|---|
| `Aventurine-3.1.5.zip` | ZIP, 465,077 bytes | League Blender addon distribution; relevant tool, not game content. Full members in appendix 7. |
| `Braum.en_US.wad.client` | WAD 3.4, 5,237,821 bytes; nine entries | Localized base/skin02/skin24 audio archives. Only base is in scope. |
| `CHR_Clash.rar` | RAR, 47,873,174 bytes; 54 files | Body OBJ, Marmoset scene, reference JPG, 44 DDS and seven TGA textures. |
| `clash new.avif` | AVIF, 45,272 bytes | Newer appearance reference; not the selected body target. |
| `clash old.jpg` | JPEG, 1,107,281 bytes | Original bald Clash reference; relevant appearance guide. |
| `Rework_Clash_Shield.blend` | Blender scene, 5,335,807 bytes | Selected shield geometry and incomplete material setup. |
| `Braum.wad/` | **Directory**, 1,843 hash-named files | Extracted main champion archive, including many other skins and shared dependencies. It is not itself a WAD archive. |

The extracted directory contains 1,298 TEX, 198 SCB, 154 ANM, 91 BIN, 34 SKL, 34 SKN, 18 BNK, 13 DDS and three extensionless files. SCB static particle meshes are essential additional formats. One extensionless entry resolves to `data/final/champions/braum.wad.subchunktoc`; the other two remain unidentified. Do not package these extraction/container implementation records as custom assets.

The original project has 1,849 files totaling 291,524,517 bytes, excluding archive members counted a second time and excluding audit outputs. The RAR contains additional `.obj`, `.tbscene` and `.tga` formats. WEM media is embedded inside BNK/WPK containers; its absence as loose files is expected.

Additional relevant files found outside P:

| Absolute path | Finding |
|---|---|
| `C:\Users\etqdo\Downloads\wadtools-0.5.7-windows-x64\Braum.wad.client` | 92,800,510-byte main archive; independent extraction matches all supplied main entries. Retain as provenance evidence. |
| `C:\Riot Games\League of Legends\Game\DATA\FINAL\Champions\Braum.wad.client` | **Absent at inspection.** Restore a pristine current game copy through the normal game repair/update workflow before testing. Do not assume the Downloads copy is the current patch. |
| `C:\Riot Games\League of Legends\Game\DATA\FINAL\Champions\Braum.en_US.wad.client` | Localized champion archive exists. English-only voice work must target this locale deliberately. |

The 50 unresolved names are listed by exact hash and original type in appendix 8. Nothing is renamed or guessed to conceal incomplete hash coverage. League WAD paths are hashed; the public hash lists plus strings recovered from actual BIN references enabled exact matches.[^2]

## Actual Braum construction

### Core files and material binding

In the following table, supplied filenames live under `P\Braum.wad\`. The right-hand column is the actual WAD-relative path, not a proposed name.

| Role / input alias | Supplied filename | Actual WAD-relative path |
|---|---|---|
| **B0** skin configuration | `eb9d53354a504663.bin` | `data/characters/braum/skins/skin0.bin` |
| **A0** animation graph | `0b01eaec2c944f55.bin` | `data/characters/braum/animations/skin0.bin` |
| Gameplay records — protected | `52d36b20890112f7.bin` | `data/characters/braum/braum.bin` |
| Skin root | `e63e7a9cbb51a42f.bin` | `data/characters/braum/skins/root.bin` |
| **M0** body/shield/Poro mesh | `fbf88fcfc8ec8dc4.skn` | `assets/characters/braum/skins/base/braum_base.skn` |
| **S0** skeleton | `9b8248658ce51711.skl` | `assets/characters/braum/skins/base/braum_base.skl` |
| Base color | `55b5d46776387920.tex` | `assets/characters/braum/skins/base/braum_base_tx_cm.tex` |
| Gloss | `9b701e6000aefb27.tex` | `assets/characters/braum/skins/base/braum_base_tx_gm.tex` |
| Poro color | `47f822652c3d3374.tex` | `assets/characters/braum/skins/base/braum_base_poro_tx_cm.tex` |
| Reflection cube | `36c71f4a03654e59.dds` | `assets/characters/braum/skins/base/braum_base_cubemap.dds` |
| Loading screen | `74d1db81b910ba99.tex` | `assets/characters/braum/skins/base/braumloadscreen.tex` |

`Characters/Braum/Skins/Skin0` binds these through `SkinMeshProperties`. It uses a legacy implicit material setup: `Texture`, `GlossTexture`, reflection map and related parameters. **There is no explicit named base `StaticMaterialDef` or shader file path in this record.** The default shader is selected by the engine; claiming a specific standalone base shader file from these inputs would be speculation. Other supplied skins contain explicit materials and can provide controlled experiments, not proof of base compatibility.

The original skin scale is approximately **1.15**, self illumination **0.7**, and `OverrideBoundingBox` is `[180,180,180]`. These are visual configuration values, not collision dimensions. Retain them initially; inspect culling if the new shield exceeds the original visual envelope. Never change gameplay radius, hitboxes or statistics to compensate for visual geometry.

The SKN is version **4.1**, with 8,837 vertices, 13,392 triangles, 52-byte vertices and two submeshes. `Braum` contains 8,068 vertices and 12,374 triangles, including the body and shield under the same original material. `Poro` contains 769 vertices and 1,018 triangles. B0 initially hides `Poro` and provides its separate texture override. Joke animation events reveal the Poro; removing it or repurposing that submesh name can create later animation defects.

### Skeleton and shield behavior

S0 contains **97 joints and a 76-entry influence palette**. Original vertices have up to four influences and normalized weights. Preserve all joints, including non-weighted buff bones, helper bones, snap bones and Poro bones. Complete names and parents are in appendix 2; native transforms are retained in `evidence/braum_skeleton.json`.

The body chain includes `Root → Spine1 → Spine2 → Spine3 → Neck → Head`, with clavicles, upper arms, forearms, hands, fingers and twist helpers. The pelvis branches from Root; legs include both upper and lower knee helpers before feet and toes. That structure differs materially from a conventional realistic human rig.

The shield is an independent animated branch: **`Origin` joint 67 → `Shield` joint 68**. `Snap_Shield2LHand` is a child of `L_Hand`; `Snap_Shield2RHand` is a child of `R_Hand`. Their names support a snapping role, but the supplied data does not establish every runtime constraint. Do not replace this system with a simple left-hand parent. All 59 inspected base animation files include a Shield track.

`Shield` also parents `Cstm_Buffbone_L_Shield_Eye`, `Cstm_Buffbone_R_Shield_Eye` and `Cstm_Buffbone_Shield_Gem`. B0 binds three idle particle keys to those bones. Remap their visual offsets/effects to shield electronics later; preserve the bones. Q, E and R also have separate particle shield geometry, so replacing only M0 leaves some original shield imagery in abilities.

### Animation coverage

A0 has **79 clip records and 1,806 blend-table records**. Its atomic resources resolve to the 59 supplied ANM files listed in appendix 3. Important actual files under `assets/characters/braum/skins/base/animations/` include:

| Behavior | Actual files / graph behavior |
|---|---|
| Idle | `braum_idle_in.anm`, `braum_idle_01_in.anm`, `braum_idle_01_loop.anm`; retain B0’s `Idle_shield` default token even though no same-name explicit clip was found. |
| Locomotion | `braum_run_02.anm`, `braum_run_slow.anm`, `braum_run_haste_01.anm`. No distinct walk ANM was identified; use the actual Run graph and its slow/haste conditions. |
| Attacks | `braum_attack_01.anm`, `braum_attack_02.anm`, `braum_attack_03.anm`, turret, critical and passive attack variants; exact spellings in appendix. |
| Q / W / R | `braum_spell1.anm`, `braum_spell2.anm`, `braum_spell4.anm`. |
| E | Directional `braum_spell3_idle…`, `braum_spell3_run…`, leap and punch variants. Idle covers ±180°, ±90° and 0°; running covers ±179°, ±90° and 0°. |
| Recall / channel | Recall, recall return, channel in/loop/windup clips, as bound in A0. |
| Other | Death, laugh, taunt, dance windup/loop, joke and joke2 sequences. |

E uses angle-driven blends through `LookAtSpellTargetAngleParametricUpdater` and conditional attack selection. One counterintuitive stock binding maps `Spell3_Punch045` to `braum_spell3_punch0-45.anm`, with the reciprocal mapping also present. Preserve the supplied mapping. Do not “correct” stock names based on intuition. Some stock animation tracks have no matching joint hash in the supplied skeleton; preserve this baseline unless a specific runtime defect establishes otherwise.

## Clash body suitability and preparation

`CHR_Clash.rar` contains `CHR_Clash/CHR_Clash.obj`, `CHR_Clash/CHR_Clash.tbscene`, a reference JPG and `CHR_Clash/Textures/…`. The OBJ exporter comment identifies a 3ds Max Wavefront export dated 2018. There is no supplied FBX, MTL, armature, skin weighting or animation. The Marmoset scene is a material/reference aid, not a League rig.

The raw OBJ has **21,324 positions, 21,255 UV coordinates, 26,495 normals and 41,175 face statements**. Blender’s inspection import yields 41,173 triangles; the two-face discrepancy must be reviewed as a degeneracy/import issue during preparation. There are 11 OBJ groups: `hjhjh_2`, `hjhjh_3`, `hjhjh_4`, `hjhjh_16`, `Object001`, `Object002`, `Object004`, `Object005`, `Object006`, `Object007` and `Object009`. These are not trustworthy semantic labels. The default inspection import produces one mesh with no materials; group reconstruction and texture assignment need deliberate work.

The source is Y-up by its coordinate distribution: approximate height 179.624, width 111.092 and depth 50.491 in unspecified OBJ units. Blender’s default OBJ axis conversion makes that height Z-up. Centimeters are plausible, but not established metadata. Match feet, pelvis, shoulders, neck and hand landmarks to the imported Braum skeleton; do not blindly apply a guessed meter conversion or bake B0’s 1.15 scale twice.

UVs span approximately U −0.9902 to 1.9863 and V 0 to 1.0001. Tiling, overlap and group-specific textures require material reconstruction followed by baking to a controlled atlas. Do not flatten the entire OBJ onto one of its source diffuse images.

The 44 DDS files include diffuse, normal, specular, mask and detail-normal maps for accessories, arms, badge, head, legs, mouth, pouches, straps and torso. Seven TGA files include eyes and six `Color_Textures3` body/gear color maps. The supplied `.tbscene` specifically binds the torso color to `Textures/Color_Textures3/CHR_Clash_Torso_DiffuseMap.tga`, not simply the similarly named torso DDS.

The readable `body_mainq` material block uses `CHR_Clash_Torso_SpecularMap.dds` channel 0 for microsurface, channel 1 for specular and channel 2 for occlusion. It uses the mask’s RGB channels for different tiled detail normals. These are source-scene channel assignments, **not a generic metallic/roughness/AO convention**. The scene records color-space flags as well; preserve their evidence and evaluate the reconstructed result before baking. A referenced custom shader, `detail_normal_rgba.frag`, is absent. Reconstruct or bake those details with available Blender nodes; this shader cannot be imported into League as a Marmoset shader.

The source is suitable as a **modeling and texture starting point**. It is unsuitable for direct League export. Body plus shield currently total approximately **53,429 triangles**, about four times the original whole Braum mesh. Use 15,000–25,000 total body-and-shield triangles as an initial art/performance target, not a claimed engine limit. Preserve the face, fingers, silhouette and shield edges while reducing hidden surfaces and redundant tessellation. Inspect final exported vertex count after UV and normal splits.

## Braum ↔ Clash rigging strategy

**Keep Braum’s animation system; adapt Clash’s mesh.** No animation retargeting from Rainbow Six is possible from the supplied inputs because none is supplied. Import S0/M0 in Aventurine’s native orientation mode, retain native bind metadata, and establish a reproducible import/export preset before reshaping the character.

| Region | Recommended adaptation and verification |
|---|---|
| Shoulders / upper arms | Broaden and raise the shoulder silhouette enough to meet Braum’s clavicles and arm reach. Use armor and sleeves to disguise the proportion change. Test overhead R, wide attack swings and E punches before refining textures. |
| Elbows / wrists / hands | Position elbow loops around the actual joint pivots, retain twist influence where useful, reshape palms to Braum’s grip and manually weight fingers. Test both shield snap poses and extreme wrist rotation. |
| Torso / neck / head | Extend torso and reposition collar/vest against the existing spine chain. Preserve the bald head silhouette; manually correct neck, jaw and collar weights. Do not stretch the face through torso weight transfer. |
| Pelvis / hips | Align the pelvis and crotch loops to the native hip motion; reshape tactical belt and pouches to avoid thigh penetration. Rigid pouches should follow a suitable bone or small stable influence set. |
| Knees / legs / feet | Account for Braum’s two knee helper segments. Maintain bending loops, plant boots on the original ground plane, and inspect slow/haste running, leaps, recall and death for skating and collapsing kneepads. |
| Equipment | Weight armor plates, pouches and radio components deliberately. Use restrained deformation; no new cloth simulation or extra dynamic skeleton is required. |
| Shield | Rigid weight 1.0 to `Shield` for frame, glass and attached CCE hardware. Align handles to actual animated hand poses rather than moving the Shield joint to fit the imported prop. |

Transfer weights from the original body only as a seed. Exclude the original shield and Poro from nearest-surface transfer. Normalize, remove negligible influences and enforce at most four influences per exported vertex, then manually correct joints. Automatic weights alone are not an acceptable final rig.

**Exporter contract:** the inspected Aventurine 3.1.5 code writes SKN 1.1 and compacts weighted joints into a new sorted influence palette. It enforces 65,535 exported vertices, 32 submeshes/materials and 256 weighted palette entries. These are the inspected exporter’s constraints, not an exhaustive statement of every modern engine limit. The input SKN being 4.1 does not automatically make an output 1.1 invalid; a vanilla round-trip and in-game test must establish acceptance.[^3]

Export the new SKN **with its paired SKL**. Compare all 97 joint names, order, parent relationships and native bind transforms against S0. Allow only the corresponding palette representation to differ unless a separately reviewed requirement exists. A byte-identical original SKL may be reused only if the exported SKN indices demonstrably address its original palette correctly. Do not delete helper bones to make a warning disappear.

Do not modify individual ANMs in the initial build. Correct geometry and weights first. If a specific motion remains unacceptable, record the exact clip, frame interval, defect and proposed cosmetic correction. An animation exception requires Very High reasoning, full transition tests, preserved duration/events and proof that it does not change gameplay timing. Wholesale animation replacement is not recommended.

## CCE shield implementation

The shield blend contains seven mesh objects, no armature and no vertex groups. It has **13,868 vertices and 12,256 triangles**. `mesh_14.001` is the main parent; its world-space bounding box is approximately 1.088 wide, 0.506 deep and 2.382 high in a metric scene with unit scale 1. Child objects carry negative scales and inherited rotations. Applying the parent/child world transform correctly is necessary to avoid mirrored normals and double rotations.

| Object | Vertices / triangles | Assigned material |
|---|---|---|
| `mesh_13.001` | 5,123 / 4,750 | `Material.015` |
| `mesh_14.001` | 7,293 / 5,874 | `Material.003` |
| `mesh_15.001` | 52 / 48 | None |
| `mesh_16` | 999 / 1,060 | None |
| `mesh_21.001` | 388 / 514 | `Material.016` |
| `mesh_22.001` | 9 / 8 | `Material.017`, no nodes |
| `mesh_23.001` | 4 / 2 | `Material.018` |

The file contains 41 material and 70 image datablocks, mostly leftovers. Only four images are packed, and those are unrelated hair maps. The active shield materials reference these missing source files:

- `000002177C0DE0C0.png`
- `00000216131DB050.png`
- `0000021700354BA0.png`

`00000216131DB050.png.001` is a duplicate datablock, with `//00000216131DB050.png` as its path. The other references point to an unavailable `C:\Users\Draft\AppData\Roaming\Ninja Ripper\…\frame_1\` location. Exact stored paths are retained in `evidence/shield_scene.json`. Recover those three source files if available. **Recommended default if they remain unavailable: author replacement shield textures from the supplied geometry and references.** Do not request all unused image datablocks or claim those files are embedded.

The shield’s UV layers `uv_0` through `uv_10` include extreme tiled values and NaNs in `uv_9`. Choose or rebuild a finite UV set after identifying geometry; the numerically plausible `uv_2` is only a candidate, not a validated final layout. Remove unused layers in the future working copy and ensure the exported single UV set is intentional.

### Transparent ballistic viewport

Separate frame and viewport faces into distinct export submeshes/materials. The source does not establish a finished dedicated glass component simply through its material names. Use the read-only geometry diagnostic in `evidence/shield_geometry_front.png` to locate surfaces, then inspect both sides and handles in Blender during preparation.

An alpha channel in a texture alone does **not** establish transparency under B0’s implicit opaque material setup. A supplied experimental donor is `data/characters/braum/skins/skin54.bin` (`07f65b37fb75e834.bin`), material `Characters/Braum/Skins/Skin54/Materials/Braum_Skin54_Ice2_inst`, referencing logical shader `Shaders/SkinnedMesh/HKG_MatCap_Diff_Mask_Alpha`. Its blend state is enabled with factors 6/7 and a four-weight skinning switch. This proves a relevant alpha-capable configuration exists in supplied data; it does not prove a direct transplant works in base Braum.

Build an early isolated glass test. Copy only required material/shader settings into a new material entry and bind the viewport by exact submesh name. Do not copy the donor’s unrelated dynamic-material conditions wholesale. The engine/global shader implementation is an external dependency; no new custom shader compilation is planned.

Test visibility from front/back and oblique angles, alpha sorting against the body and arcs, depth writes, fog/stealth transitions and ground telegraphs behind the shield. Prefer one simple pane with restrained tint/reflection. Physically accurate refraction, multi-layer thick glass and full Rainbow Six renderer fidelity are not promised. If a stable skinned translucent material cannot be established, use an open aperture with a subtle modeled rim or a restrained visual glass approximation. Never fill the entire viewport with an opaque white electrical plane.

Braum E remains the same directional defensive ability. Keep its distinct first-block, sustained active, subsequent-block and expiration states. Add arcs along shield edges and CCE emitters while preserving the visible outline and facing direction. Do not add a deployable shield, continuous taser beam, new collision surface or new gameplay interaction.

## Ability VFX plan

B0’s `Characters/Braum/Skins/Skin0/Resources` maps effect keys to definitions in **13 actual owner BINs**. Appendix 1 gives the complete long `braum_multi_skins_…bin` paths; appendix 4 gives every key, definition, owner hash and dependency. Short owner hashes below are exact supplied identifiers and can be looked up without guessing a filename.

The recommended architecture is to clone only changed VFX definitions to new logical entries inside the modified skin0 BIN, preserve existing effect keys, and point those base resolver bindings at the clones. Rewire any cloned child references explicitly. Keep original linked BINs for unchanged resources. This avoids changing shared multi-skin definitions and avoids relying on unverified duplicate-entry precedence.

| Area | Actual resolver family / dependencies | Proposed cosmetic change | Difficulty and reason |
|---|---|---|---|
| Passive | `Braum_P_…` stack1/2/3/3_warning/4, stun, debuff timers, cooldown-hit and Poro-mustache effects; exact spelling in appendix | Electrical stack marks, brief white/blue discharge on stun, restrained cooldown indication | **MODERATE.** Preserve four-stack progression, near-stun warning, stun recognition and cooldown state. A static “electric aura” would conceal state changes. |
| Q | `Braum_Q_mis`, hit/monster-hit systems and `Braum_Q_Shield_base_cas`; SCB projectile/shield geometry and trails/textures | Compact electrical projectile and discharge, with original travel/readability envelope | **MODERATE.** Geometry, trail and hit systems need coordinated changes. A continuous hitscan-looking beam is **NOT RECOMMENDED** because Q remains a traveling projectile. |
| W | Cast, `Braum_W_Dash_Land`, landing indicator and shield-buff family | Small tactical pulse and restrained electrical landing accent | **MODERATE** for the complete conversion; tint-only is **EASY**. Preserve ally landing and buff communication; avoid making W resemble an offensive stun. |
| E | `Braum_E_Shield_cas`, block-BA/block-spell variants, self effect, dummy timer, first-block/end, shield-end and speed buff | Energized CCE outline, controlled white/blue arcs, differentiated activation/first block/impact/expiry | **DIFFICULT.** Multiple state transitions, independently animated shield, attachment alignment and transparency interact. Persistent loop cleanup must work on death and cancellation. |
| R | Missile/small missile/rock child, first/small knockup, endcaps, PBAOE cast, slow/freeze, team telegraphs, zone/pulse sound and shield-cast systems | Electrical ground shockwave with retained width, travel, endpoints and lingering-zone clarity | **HIGH RISK** for full geometry replacement. Retinting alone is easier but leaves ice meshes. Ground mesh animation, child effects and team-specific telegraphs must remain synchronized. |
| Attacks | `Braum_BA_Hit_01/02/03`, critical hit and trail families | Restrained shield impact sparks, short electrical accents where appropriate | **EASY–MODERATE.** Preserve attack readability and passive-proc distinction; inspect animation-triggered trails. |
| Idle / recall / emotes | Shield gem/eyes, recall and joke systems, contextual action data | Replace shield idle glow with electronics; retain recall/joke behavior initially | **MODERATE.** Avoid persistent particles at old shield-eye positions and preserve Poro visibility events. |

The three VFX definitions embedded directly in B0 are `Braum_E_Shield_cas`, `Braum_Q_Shield_base_cas` and `Braum_R_shield_Base_cas` families. Most remaining definitions live elsewhere. Some base effects legitimately reference `skin10` textures. A texture’s directory does not establish the selected skin slot; changing it globally could affect multiple skins.

R has an important hidden dependency: `assets/characters/braum/skins/base/particles/braum_base_r_ice_shard.skn`, `.skl` and `.anm`, supplied as `4e39c2b7f8fb2101.skn`, `8d71893c571f5f5d.skl` and `82c69c58b966af73.anm`. Recoloring sprites will not remove this animated ice geometry. There are also SCB static particle meshes. For the first electrical R, retain the tested movement envelope and progressively substitute low-profile electrical ground visuals; do not remove the lingering zone cue.

Preserve ally/enemy telegraph differentiation, endpoint placement, visibility duration and impact hierarchy. Avoid global shared texture overwrites; give custom textures new verified WAD paths. Do not alter the protected champion/spell BIN, missile behavior, damage, hitboxes, effect trigger times or animation event timing. VFX rendering changes must stay aligned with the existing gameplay.

## Audio plan

Base Braum exposes **53 SFX events and 41 VO events** through B0. All 94 names were matched to supplied Wwise event IDs. Wwise event hashing here is lowercase **FNV-1**, distinct from BIN’s FNV-1a and WAD’s XXH64. The archive path segment `wwise2016` is not evidence that Wwise 2016 should be installed. The inspected banks have **BKHD version 145**.

| Container | Actual path / supplied identifier | Contents and replacement scope |
|---|---|---|
| SFX media bank | `assets/sounds/wwise2016/sfx/characters/braum/skins/base/braum_base_sfx_audio.bnk` — `668ac17b89a8d8ea.bnk` | Bank ID 3580963463; DIDX/DATA with **114 media**. Preferred unit for selected SFX replacement. |
| SFX events bank | Same directory, `braum_base_sfx_events.bnk` — `6a0cd1a55c7df583.bnk` | Bank ID 924561672; 368 HIRC objects, including 53 events. Preserve when media-only replacement is sufficient. |
| Base VO events | `assets/sounds/wwise2016/vo/en_us/characters/braum/skins/base/braum_base_vo_events.bnk` — `17802985c2cae524.bnk` | Localized WAD; 41 events and their container graph. |
| Base VO audio bank | Same VO directory, `braum_base_vo_audio.bnk` — `1edf235828b09246.bnk` | **48 bytes, BKHD only. No voice samples.** Replacing it alone cannot replace the dialogue. |
| Base VO media | Same VO directory, `braum_base_vo_audio.wpk` — `4826a53ff12ced20.wpk` | WPK `r3d2`, version 1, **115 media entries**. Actual localized voice media. |

All 114 reachable SFX media and all 115 reachable VO media are present. Every inspected payload is **mono, 44,100 Hz Wwise Vorbis**, RIFF format tag `0xffff`. The event traversal identifies possible media variants; runtime switches, randomness and contextual conditions determine which plays. Twenty-one SFX media IDs and seven VO media IDs are shared across multiple events. Replacing shared media affects every listed event, not just the event used to find it.[^4]

One graph edge remains unresolved: `Play_sfx_Braum_BraumRWrapper_OnHit`, event **4008465767**, references action **792732449**, which targets object **222662794**. That object is absent from every supplied BNK. It may be external or stale. Leave the event untouched and identify its audible behavior from a clean baseline/global bank inspection if that exact cue becomes necessary. This gap does not prevent replacing the separately resolved R cast and pulse cues.

| Candidate cue | Confirmed event ID / media example | Recommended scope |
|---|---|---|
| Q discharge | `Play_sfx_Braum_BraumQ_OnCast` **4193070101** → media 22215941, 40941018, 41688918, 45474400, 47820709 | **MODERATE.** Two listed media also serve attacks; use the shared-media appendix before selecting samples. |
| Q impact | `Play_sfx_Braum_BraumQMissile_hit` **656004614** → 103689118, 680861900, 1071447244 | **MODERATE.** Audition variants, preserve impact duration and loudness. |
| Shield activation | `Play_sfx_Braum_BraumEShieldBuff_OnBuffActivate` **2600776008** → 541689440, 557873384 | **MODERATE.** Electrical charge/loop must respect original stop/deactivate behavior. |
| Large shield block | `Play_sfx_Braum_BraumEShieldBuff_block_large` **1065834682** → 169392210, 278315751, 491993041 | **MODERATE.** Retain distinction from small/layered block cues. |
| R cast | `Play_sfx_Braum_BraumRWrapper_OnCast` **1869821925** → 309351640 | **MODERATE.** Short electrical shockwave cue; retain envelope and spatial behavior. |
| Passive-related attack hit | `Play_sfx_Braum_BraumBasicAttackPassiveOverride_hit` **3139578176** → 510145146, 786873024, 1041470451 | **MODERATE.** Verify actual trigger by audition and gameplay; do not label it the universal fourth-stack stun solely from its name. |
| General attacks / equipment | Attack cast/hit, shield override, critical and tower events in appendix | **MODERATE–DIFFICULT** due to shared variants. No dedicated equipment-footstep event was established in the base list; avoid global footstep replacement. |
| Full voice-over | 41 localized events, 115 media and contextual selection | **HIGH RISK / optional.** Requires a coherent recording set and contextual mapping; preserve spatial modes and special interaction semantics. |

Use newly recorded or otherwise available Clash-inspired electricity and voice sources; no Clash audio was supplied and none is assumed available. Recommended audio authoring masters are lossless PCM WAV, optionally 24-bit/48 kHz for editing. For this exact replacement set, encode delivery WEM to the observed **mono 44.1 kHz** configuration unless an individually tested requirement justifies a change. Preserve useful duration, leading silence, gain and loop metadata. Renaming WAV/OGG/MP3 to `.wem` does not create compatible Wwise media.

Quartz’s SoundBanks functionality and `bnk-extract-GUI` are candidates for extraction, replacement and repacking. They are sufficient for bank operations if they pass a version-145 no-op round-trip and a single-sample replacement test. **Compatible WEM encoding is a separate requirement.** Do not infer encoder compatibility from a successful bank parse. Wwise authoring is conditional if the selected tooling cannot produce accepted media; its required version must be established from the encoding workflow, not from the directory name. `wwiser` is useful for event inspection and does not edit banks.[^5]

Retain event IDs, play/stop actions, switches, random containers, attenuation and 2D/3D behavior whenever replacing only sound content. Preserve W activation/deactivation stop events, E shutdown and death/cancellation cleanup. If a shared media replacement must affect only one event, cloning containers/IDs becomes a separate difficult task; defer that complexity for the first release.

## `.fantome` architecture

The final artifact must be **`Braum_Clash.fantome`**. Use a conventional single-layer ZIP-based package with `META/info.json` and WAD-targeted replacements. Do not substitute `.modpkg`, introduce optional-layer extensions, or package the entire project. The established format supports a directory named after a target WAD or a packed replacement WAD containing only changed entries.[^6]

### Proposed exact output namespace

These names are design choices for future assets, clearly distinct from discovered vanilla names:

| Alias | Exact future path relative to P |
|---|---|
| **PKG** | `build/package/` |
| **MAIN** | `build/package/WAD/Braum.wad.client/` |
| **BASE** | `build/package/WAD/Braum.wad.client/assets/characters/braum/skins/base/braum_clash/` |
| **OUTBIN** | `build/package/WAD/Braum.wad.client/data/characters/braum/skins/skin0.bin` |
| **VOICE** | `build/package/WAD/Braum.en_US.wad.client/assets/sounds/wwise2016/vo/en_us/characters/braum/skins/base/` |

The intended package tree is:

```text
Braum_Clash.fantome
├── META/
│   └── info.json
└── WAD/
    ├── Braum.wad.client/
    │   ├── data/characters/braum/skins/skin0.bin
    │   └── assets/
    │       ├── characters/braum/skins/base/braum_clash/
    │       │   ├── braum_clash.skn
    │       │   ├── braum_clash.skl
    │       │   ├── body.tex
    │       │   ├── body_gloss.tex
    │       │   ├── shield_frame.tex
    │       │   ├── shield_glass.tex
    │       │   ├── electric_arcs.tex
    │       │   └── ground_pulse.tex
    │       └── sounds/wwise2016/sfx/characters/braum/skins/base/
    │           └── braum_base_sfx_audio.bnk
    └── Braum.en_US.wad.client/        [only if VO is implemented]
        └── assets/sounds/wwise2016/vo/en_us/characters/braum/skins/base/
            └── braum_base_vo_audio.wpk
```

Include only assets actually used by the final build; unused glass/future VFX files must be omitted. Conditional additional particle outputs are specified in Stage 7. Metadata `image.png` is optional and not required for the cosmetic result. A proposed metadata record is:

```json
{
  "Name": "Braum - Clash",
  "Author": "Ethan",
  "Version": "1.0.0",
  "Description": "Base Braum cosmetic conversion using Clash-inspired tactical equipment, CCE shield and electrical effects."
}
```

OUTBIN points `SimpleSkin` and `Skeleton` to the new matched files in BASE. It binds the new texture/material entries and custom VFX while retaining all gameplay-facing keys, animation graph links and unchanged dependencies. `Braum`, `ShieldFrame`, `ShieldGlass` and retained `Poro` are the proposed export submesh names. Material overrides must match those names exactly. Preserve Poro’s original texture reference through the base game dependency.

### Path, hash and override rules

WAD lookup uses the canonical game path’s XXH64 identity. BIN entry/field identity uses separate 32-bit hashing. Use forward slashes and consistent lowercase game paths when hashing; preserve known logical references through a format-aware writer. Verify each packaged entry hash and every new reference rather than relying on the visual folder name.

For an existing path, the mod entry replaces the corresponding base archive entry. For a new path, the mod adds an entry that must be referenced by OUTBIN or a reachable custom resource. New textures dropped into a folder with no updated reference will never appear. A replacement `skin0.bin` is a whole entry: preserve unknown and unrelated fields through a lossless parse/write workflow, not a hand-written minimal imitation.

The very long shared BIN names can exceed normal Windows path-component limits. Read them by supplied hash filenames and retain their game-path mapping; avoid trying to materialize every long name as a Windows file. This design does not modify those shared owner files. Modern packers may support raw 16-hex-hash entries, but **do not assume every existing manager recognizes `hash.tex` as a precomputed hash**. Prefer readable verified paths for this package or use a tested WAD writer when a raw-hash replacement is unavoidable.[^7]

Do not include the source RAR/BLEND/OBJ/TGA collection, addon ZIP, audit directory, all 1,843 extracted files, untouched ANMs, untouched root/gameplay BINs, unrelated skins, `*.wad.subchunktoc`, unknown extraction entries, development renders, shader caches, loose WAV masters or unused banks. A modified audio bank or WPK contains its needed unchanged media internally because that container is the replacement unit; this does not justify shipping every champion bank.

No changes to `RAW/` are needed. The ZIP root must immediately contain `META/` and `WAD/`, without an extra `Braum_Clash/` wrapper. File extension must be `.fantome`, not `.fantome.zip`. Validate archive CRCs, duplicate case-insensitive paths, allowed WAD targets, reference closure, SHA-256 manifest and an import/reimport using the already-owned manager. That manager’s exact implementation was not identified; the conventional layout is the recommended baseline, and its acceptance remains a test gate.
